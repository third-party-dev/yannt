# Pickle Use Cases

```sh
pparse --breakpoint pickle parse ./models/bert/bert-AutoModel.complete/data.pkl
```

```sh
pparse pickle parse --print ./models/bert/bert-AutoModel.complete/data.pkl
```


```text
# pparse pickle parse --print ./models/bert/bert-AutoModel.complete/data.pkl
Parsing pickle from: ./models/bert/bert-AutoModel.complete/data.pkl
transformers.models.bert.modeling_bert.BertModel(
  *(  # ARG
    [
    ]
  )  # End of ARG

  # STATE
  {
    _is_hf_initialized: True
    _no_split_modules: __builtin__.set(
      *(  # ARG
        [
          [
            BertLayer
            BertEmbeddings
          ]
        ]
      ) 
```
[]{#thirdparty.pparse.lib}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib

docstring will go here

::: {.lefttable}
| Name                                                                              |
| --------------------------------------------------------------------------------- |
| [`log`](#thirdparty.pparse.lib.log)                                               |
| [`AGAIN`](#thirdparty.pparse.lib.AGAIN)                                           |
| [`ASCEND`](#thirdparty.pparse.lib.ASCEND)                                         |
| [`NEXT`](#thirdparty.pparse.lib.NEXT)                                             |
| [`__all__`](#thirdparty.pparse.lib.__all__)                                       |
| [`EndOfDataException`](#thirdparty.pparse.lib.EndOfDataException)                 |
| [`EndOfNodeException`](#thirdparty.pparse.lib.EndOfNodeException)                 |
| [`UnsupportedFormatException`](#thirdparty.pparse.lib.UnsupportedFormatException) |
| [`BufferFullException`](#thirdparty.pparse.lib.BufferFullException)               |
| [`NodeContext`](#thirdparty.pparse.lib.NodeContext)                               |
| [`UNLOADED_VALUE`](#thirdparty.pparse.lib.UNLOADED_VALUE)                         |
| [`RecursionControl`](#thirdparty.pparse.lib.RecursionControl)                     |
| [`Node`](#thirdparty.pparse.lib.Node)                                             |
| [`Reader`](#thirdparty.pparse.lib.Reader)                                         |
| [`Cursor`](#thirdparty.pparse.lib.Cursor)                                         |
| [`Range`](#thirdparty.pparse.lib.Range)                                           |
| [`Data`](#thirdparty.pparse.lib.Data)                                             |
| [`HttpCachedData`](#thirdparty.pparse.lib.HttpCachedData)                         |
| [`FileData`](#thirdparty.pparse.lib.FileData)                                     |
| [`FileMmapData`](#thirdparty.pparse.lib.FileMmapData)                             |
| [`BytesIoData`](#thirdparty.pparse.lib.BytesIoData)                               |
| [`Extraction`](#thirdparty.pparse.lib.Extraction)                                 |
| [`BytesExtraction`](#thirdparty.pparse.lib.BytesExtraction)                       |
| [`Parser`](#thirdparty.pparse.lib.Parser)                                         |
| [`Tensor`](#thirdparty.pparse.lib.Tensor)                                         |
| [`PparseXml`](#thirdparty.pparse.lib.PparseXml)                                   |
| [`node`](#thirdparty.pparse.lib.node)                                             |
| [`exceptions`](#thirdparty.pparse.lib.exceptions)                                 |
| [`extraction`](#thirdparty.pparse.lib.extraction)                                 |
| [`data`](#thirdparty.pparse.lib.data)                                             |
| [`node_context`](#thirdparty.pparse.lib.node_context)                             |
| [`constants`](#thirdparty.pparse.lib.constants)                                   |
| [`parser`](#thirdparty.pparse.lib.parser)                                         |
| [`pparsexml`](#thirdparty.pparse.lib.pparsexml)                                   |
| [`tensor`](#thirdparty.pparse.lib.tensor)                                         |
| [`reader`](#thirdparty.pparse.lib.reader)                                         |
:::

[]{#thirdparty.pparse.lib.log}

:::: {.member-rule}
::::

#### log

```text
thirdparty.pparse.lib.log
```

[]{#thirdparty.pparse.lib.AGAIN}

:::: {.member-rule}
::::

#### AGAIN

```text
thirdparty.pparse.lib.AGAIN
```

[]{#thirdparty.pparse.lib.ASCEND}

:::: {.member-rule}
::::

#### ASCEND

```text
thirdparty.pparse.lib.ASCEND
```

[]{#thirdparty.pparse.lib.NEXT}

:::: {.member-rule}
::::

#### NEXT

```text
thirdparty.pparse.lib.NEXT
```

[]{#thirdparty.pparse.lib.__all__}

:::: {.member-rule}
::::

#### __all__

```text
thirdparty.pparse.lib.__all__
```

[]{#thirdparty.pparse.lib.EndOfDataException}

:::: {.member-rule}
::::

#### EndOfDataException

```text
thirdparty.pparse.lib.EndOfDataException
```

Raised when the end of the data currently available is reached.

[]{#thirdparty.pparse.lib.EndOfNodeException}

:::: {.member-rule}
::::

#### EndOfNodeException

```text
thirdparty.pparse.lib.EndOfNodeException
```

Raised when the parser has finished processing the current node.

[]{#thirdparty.pparse.lib.UnsupportedFormatException}

:::: {.member-rule}
::::

#### UnsupportedFormatException

```text
thirdparty.pparse.lib.UnsupportedFormatException
```

Raised when the observed data is not recognized by the parser.

[]{#thirdparty.pparse.lib.BufferFullException}

:::: {.member-rule}
::::

#### BufferFullException

```text
thirdparty.pparse.lib.BufferFullException
```

Raised when a buffer cannot accept additional data.

[]{#thirdparty.pparse.lib.NodeContext}

:::: {.member-rule}
::::

#### NodeContext

```text
thirdparty.pparse.lib.NodeContext
```

Holds the parser state and reader position associated with a single ``Node``.

``NodeContext`` is the glue between a ``Node`` and its ``Parser``.  It
owns the parser's state stack for the node and provides pass-through
reader methods so that parser state objects do not need a direct
reference to the underlying ``Reader``. It's also an all in one reference
to cut once parsing is complete and we need to restore memory.

**Parameters:**

  - `parent` - Optional[Node] - The parent ``Node``, or ``None`` for the root.
  - `reader` - Reader - The ``Reader`` positioned at the start of this node's data.
  - `parser` - Parser - The ``Parser`` responsible for interpreting this node.

[]{#thirdparty.pparse.lib.UNLOADED_VALUE}

:::: {.member-rule}
::::

#### UNLOADED_VALUE

```text
thirdparty.pparse.lib.UNLOADED_VALUE
```

[]{#thirdparty.pparse.lib.RecursionControl}

:::: {.member-rule}
::::

#### RecursionControl

```text
thirdparty.pparse.lib.RecursionControl
```

Configurable guard that limits how deeply ``Node.load`` recurses.

``RecursionControl`` is passed to ``Node.load`` to give callers
fine-grained control over which parts of the parse tree are eagerly
expanded.  At each node, ``stopped`` is called to decide whether
recursion should halt.

**Parameters:**

  - `min_depth` - int - Nodes at depths below this value are always loaded,
ignoring ``max_depth`` and the callback.
  - `max_depth` - int - Nodes at depths above this value are never loaded.
  - `callback` - Optional[Callable[[Node], bool]] - An optional callable that receives a ``Node`` and returns
``True`` to stop recursion at that node.  Only called when
``min_depth <= cur_depth <= max_depth``.

**See Also:**

  - {'name': None, 'description': 'Something to notice that is a bit longer and probably should use\na newline to show its all a single item.'}
  - {'name': 'thing2', 'description': '[Search Link](https://google.com)'}

[]{#thirdparty.pparse.lib.Node}

:::: {.member-rule}
::::

#### Node

```text
thirdparty.pparse.lib.Node
```

A single node in the pparse parse-tree.

A ``Node`` represents a region of binary data that a ``Parser`` knows about
and _possibly_ how to interpret.  Its ``_value`` starts as ``UNLOADED_VALUE`` and is
populated lazily on first access via the ``value`` property, which
triggers ``load()``.

Each node owns a ``NodeContext`` that carries the parser's state machine (for the Node)
and a ``Reader`` positioned at the start of the node's data offset.

**Parameters:**

  - `reader` - Reader - A ``Reader`` positioned at the start of this node's data.
  - `parser` - Parser - The ``Parser`` responsible for interpreting this node.
  - `default_value` - Any - Initial value; defaults to ``UNLOADED_VALUE`` so that
the node is parsed lazily on first access.
  - `parent` - Optional[Node] - The parent ``Node``, or ``None`` for the root.
  - `ctx_class` - Optional[Type[NodeContext]] - An optional ``NodeContext`` subclass to instantiate instead
of the default ``NodeContext``.
  - `ctx_args` - Dict[str, Any] - Extra keyword arguments forwarded to ``ctx_class``.

[]{#thirdparty.pparse.lib.Reader}

:::: {.member-rule}
::::

#### Reader

```text
thirdparty.pparse.lib.Reader
```

Abstract interface for objects that provide sequential and random access to binary data.

Both ``Cursor`` and ``Range`` implement this interface.  Callers should
program to ``Reader`` wherever possible so that either concrete type can
be substituted transparently.

[]{#thirdparty.pparse.lib.Cursor}

:::: {.member-rule}
::::

#### Cursor

```text
thirdparty.pparse.lib.Cursor
```

A stateful read pointer into a ``Data`` object.

``Cursor`` tracks the current byte offset and uses ``Data`` backend
for all I/O access.  It does not enforce any boundary; that
responsibility belongs to ``Range``.

**Parameters:**

  - `data` - Any - The backing ``Data`` object that performs actual I/O.
  - `offset` - int - Initial byte offset within ``data``.

[]{#thirdparty.pparse.lib.Range}

:::: {.member-rule}
::::

#### Range

```text
thirdparty.pparse.lib.Range
```

A bounded view over a region of a data source.

``Range`` wraps a ``Cursor`` and clamps all seek, skip, peek, and read
operations to the half-open interval ``[start, start + length]``.  It
does not duplicate any I/O logic — all actual data access is delegated
to the underlying cursor.

**Parameters:**

  - `cursor` - Cursor - A ``Cursor`` whose current position marks the start of the range.
  - `length` - int - Number of bytes in the range.  Must be >= 0.
  - `offset` - int - If >= 0, the initial read position within the range; otherwise
defaults to the start of the range.

**Raises:**


[]{#thirdparty.pparse.lib.Data}

:::: {.member-rule}
::::

#### Data

```text
thirdparty.pparse.lib.Data
```

Abstract data backend that provides I/O operations to ``Cursor`` objects.

``Data`` subclasses own the access to persistent storage (file, HTTP URL, mmap,
in-memory ``BytesIO``).  ``Data`` implements ``peek`` for non-advancing reads;
seek and read are provided with default implementations built on top of
``peek``.

The design keeps offset tracking out of ``Data``: a ``Cursor`` manages
the current position and passes itself to ``Data`` methods as a handle. A
``Cursor`` is not unlike a user-space defined file descriptor.

[]{#thirdparty.pparse.lib.HttpCachedData}

:::: {.member-rule}
::::

#### HttpCachedData

```text
thirdparty.pparse.lib.HttpCachedData
```

``Data`` backend that fetches remote content via HTTP with a local chunk cache.

Downloads the target resource in fixed-size chunks and keeps a bounded
LRU-style cache in memory.  Supports servers that advertise
``Accept-Ranges: bytes``; falls back to a "download from beginning" when Range
requests are not available.

**Parameters:**

  - `url` - str - The full HTTP/HTTPS URL of the target resource.
  - `chunk_size` - int - Size of each cache chunk in bytes. (Ideally close to page size or hardware cache size)
  - `chunk_max_count` - int - Maximum number of chunks to keep in the cache (consider SDRAM available to process).
  - `session` - Optional[Any] - An optional ``requests.Session`` to use for HTTP calls.
A new session is created when this is ``None``.

**Raises:**


[]{#thirdparty.pparse.lib.FileData}

:::: {.member-rule}
::::

#### FileData

```text
thirdparty.pparse.lib.FileData
```

``Data`` backend that reads from a local file via a buffer.

Opens the file in binary read mode and uses ``seek()`` + ``read()`` syscalls
for each access.  The file object is kept open for the lifetime of the
instance (shared with all associated ``Reader``s).

**Parameters:**

  - `path` - Optional[str] - Absolute or relative path to the file to read.

**Raises:**


[]{#thirdparty.pparse.lib.FileMmapData}

:::: {.member-rule}
::::

#### FileMmapData

```text
thirdparty.pparse.lib.FileMmapData
```

``Data`` backend that memory-maps a local file for zero-copy access.

Uses ``mmap`` and a ``memoryview`` overlay so that slice operations
return a ``memoryview`` without copying bytes.  This is the fastest
backend for local files when random access patterns are common.

Note: Untested in production use. (Unused/Legacy)

**Parameters:**

  - `path` - Optional[str] - Absolute or relative path to the file to map.

**Raises:**


[]{#thirdparty.pparse.lib.BytesIoData}

:::: {.member-rule}
::::

#### BytesIoData

```text
thirdparty.pparse.lib.BytesIoData
```

``Data`` backend backed by an in-memory ``io.BytesIO`` buffer.

Useful when the raw bytes are already in memory, e.g. when a file inside
a ZIP archive has been decompressed into a buffer and needs to be parsed
as its own extraction.

**Parameters:**

  - `bytes_io` - Optional[io.BytesIO] - The ``BytesIO`` buffer to read from.

**Raises:**


[]{#thirdparty.pparse.lib.Extraction}

:::: {.member-rule}
::::

#### Extraction

```text
thirdparty.pparse.lib.Extraction
```

Abstract representation of a named data source with zero or more attached parsers.

An ``Extraction`` couples a raw data source (file, URL, in-memory buffer)
with the parsers that know how to interpret it.  Subclasses implement
``open()`` to return a ``Reader`` positioned at the start of the data.

**Parameters:**

  - `name` - Optional[str] - A human-readable name for this extraction (e.g. a filename).
  - `source` - Optional[Extraction] - The parent this ``Extraction`` was derived from, if any.

[]{#thirdparty.pparse.lib.BytesExtraction}

:::: {.member-rule}
::::

#### BytesExtraction

```text
thirdparty.pparse.lib.BytesExtraction
```

An ``Extraction`` backed by an existing ``Reader`` (bytes already in hand).

Used when the raw data is accessible via a ``Reader`` — for example, a
``Range`` wrapping a memory-mapped file or a ``BytesIoData`` cursor.
Exactly one of ``source`` or ``reader`` must be provided. Note: Use only source xor reader, not both (i.e. one and only one must be set).

**Parameters:**

  - `name` - Optional[str] - A human-readable name for this extraction (e.g. relative filename).
  - `source` - Optional[Extraction] - Parent ``Extraction`` to ``open`` a ``Reader``.
  - `reader` - Optional[Reader] - An existing ``Reader`` to use directly.

**Raises:**


[]{#thirdparty.pparse.lib.Parser}

:::: {.member-rule}
::::

#### Parser

```text
thirdparty.pparse.lib.Parser
```

Abstract base class for format-specific parsers attached to an ``Extraction``.

A ``Parser`` is responsible for lazily interpreting the bytes provided by
its ``Extraction`` and populating a tree of ``Node`` objects.  Concrete
subclasses override ``match_extension`` and ``match_magic`` so the framework
can auto-detect the right parser for a given data source.

If ``base_state_cls`` is provided the constructor automatically discovers
all transitive subclasses and registers them by name in ``_all_states``,
enabling state lookup by string name via ``_init_state_as_cls``. (Required for
XML load and resume.)

**Parameters:**

  - `source` - Extraction - The ``Extraction`` this parser will consume.
  - `id` - str - A unique string identifier for this parser within the extraction.
  - `base_state_cls` - Optional[Type[Any]] - Optional base class for the parser's state machine.
All subclasses of this class are registered in ``_all_states``.

**Raises:**


[]{#thirdparty.pparse.lib.Tensor}

:::: {.member-rule}
::::

#### Tensor

```text
thirdparty.pparse.lib.Tensor
```

Abstract representation of a typed, shaped tensor stored in a binary format.

Subclasses are expected to implement all abstract methods to provide
access to the underlying data in a format-independent way.  The class
also provides lookup tables that map pparse dtype strings to struct
format characters, element sizes, and NumPy dtypes.

[]{#thirdparty.pparse.lib.PparseXml}

:::: {.member-rule}
::::

#### PparseXml

```text
thirdparty.pparse.lib.PparseXml
```

Coordinator for XML-driven import of a pparse parse session.

``PparseXml`` is instantiated once per ``<pparse />`` XML document.  It
maintains a mapping from numeric result IDs to the ``BytesExtraction``
objects that own those results, acting as a cross-reference resolver
during the recursive import of ``<node />`` elements.

**Parameters:**

  - `xml` - Any - The root XML node (or string) representing the ``<pparse />``
document being imported.

[]{#thirdparty.pparse.lib.node}

:::: {.member-rule}
::::

#### node

```text
thirdparty.pparse.lib.node
```

Node — the core data unit of the pparse parse-tree.

[]{#thirdparty.pparse.lib.exceptions}

:::: {.member-rule}
::::

#### exceptions

```text
thirdparty.pparse.lib.exceptions
```

Exceptions raised by the pparse parsing framework.

[]{#thirdparty.pparse.lib.extraction}

:::: {.member-rule}
::::

#### extraction

```text
thirdparty.pparse.lib.extraction
```

Extraction classes that couple a data source to one or more parsers.

[]{#thirdparty.pparse.lib.data}

:::: {.member-rule}
::::

#### data

```text
thirdparty.pparse.lib.data
```

Data source implementations that back ``Cursor`` and ``Range`` objects.

[]{#thirdparty.pparse.lib.node_context}

:::: {.member-rule}
::::

#### node_context

```text
thirdparty.pparse.lib.node_context
```

NodeContext — per-node parser state machine and parser bookkeeping.

[]{#thirdparty.pparse.lib.constants}

:::: {.member-rule}
::::

#### constants

```text
thirdparty.pparse.lib.constants
```

pparse constants

[]{#thirdparty.pparse.lib.parser}

:::: {.member-rule}
::::

#### parser

```text
thirdparty.pparse.lib.parser
```

Base ``Parser`` class for format-specific extraction parsers.

[]{#thirdparty.pparse.lib.pparsexml}

:::: {.member-rule}
::::

#### pparsexml

```text
thirdparty.pparse.lib.pparsexml
```

XML-driven import/export helpers that bridge pparse's object model and XML.

[]{#thirdparty.pparse.lib.tensor}

:::: {.member-rule}
::::

#### tensor

```text
thirdparty.pparse.lib.tensor
```

Abstract base class for tensor values parsed from binary model formats.

[]{#thirdparty.pparse.lib.reader}

:::: {.member-rule}
::::

#### reader

```text
thirdparty.pparse.lib.reader
```

Reader interface and Cursor/Range implementations for binary data access.

[]{#thirdparty.pparse.lib.node_context.NodeContext}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.node_context.NodeContext

```python
class NodeContext()
```

docstring will go here

::: {.lefttable}
| Name                                                                                   |
| -------------------------------------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.node_context.NodeContext.__init__)                 |
| [`_reader`](#thirdparty.pparse.lib.node_context.NodeContext._reader)                   |
| [`_state_stack`](#thirdparty.pparse.lib.node_context.NodeContext._state_stack)         |
| [`_parent`](#thirdparty.pparse.lib.node_context.NodeContext._parent)                   |
| [`_start`](#thirdparty.pparse.lib.node_context.NodeContext._start)                     |
| [`_end`](#thirdparty.pparse.lib.node_context.NodeContext._end)                         |
| [`_parser`](#thirdparty.pparse.lib.node_context.NodeContext._parser)                   |
| [`_descendants`](#thirdparty.pparse.lib.node_context.NodeContext._descendants)         |
| [`parent`](#thirdparty.pparse.lib.node_context.NodeContext.parent)                     |
| [`parent_ctx`](#thirdparty.pparse.lib.node_context.NodeContext.parent_ctx)             |
| [`reader`](#thirdparty.pparse.lib.node_context.NodeContext.reader)                     |
| [`parser`](#thirdparty.pparse.lib.node_context.NodeContext.parser)                     |
| [`_init_state`](#thirdparty.pparse.lib.node_context.NodeContext._init_state)           |
| [`_init_states`](#thirdparty.pparse.lib.node_context.NodeContext._init_states)         |
| [`_next_state`](#thirdparty.pparse.lib.node_context.NodeContext._next_state)           |
| [`_next_states`](#thirdparty.pparse.lib.node_context.NodeContext._next_states)         |
| [`state`](#thirdparty.pparse.lib.node_context.NodeContext.state)                       |
| [`_pop_state`](#thirdparty.pparse.lib.node_context.NodeContext._pop_state)             |
| [`set_remaining`](#thirdparty.pparse.lib.node_context.NodeContext.set_remaining)       |
| [`mark_end`](#thirdparty.pparse.lib.node_context.NodeContext.mark_end)                 |
| [`mark_field_start`](#thirdparty.pparse.lib.node_context.NodeContext.mark_field_start) |
| [`field_start`](#thirdparty.pparse.lib.node_context.NodeContext.field_start)           |
| [`dup`](#thirdparty.pparse.lib.node_context.NodeContext.dup)                           |
| [`tell`](#thirdparty.pparse.lib.node_context.NodeContext.tell)                         |
| [`seek`](#thirdparty.pparse.lib.node_context.NodeContext.seek)                         |
| [`skip`](#thirdparty.pparse.lib.node_context.NodeContext.skip)                         |
| [`peek`](#thirdparty.pparse.lib.node_context.NodeContext.peek)                         |
| [`read`](#thirdparty.pparse.lib.node_context.NodeContext.read)                         |
| [`left`](#thirdparty.pparse.lib.node_context.NodeContext.left)                         |
:::

[]{#thirdparty.pparse.lib.node_context.NodeContext.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.node_context.NodeContext.__init__
```

[]{#thirdparty.pparse.lib.node_context.NodeContext._reader}

:::: {.member-rule}
::::

#### _reader

```text
thirdparty.pparse.lib.node_context.NodeContext._reader
```

[]{#thirdparty.pparse.lib.node_context.NodeContext._state_stack}

:::: {.member-rule}
::::

#### _state_stack

```text
thirdparty.pparse.lib.node_context.NodeContext._state_stack
```

[]{#thirdparty.pparse.lib.node_context.NodeContext._parent}

:::: {.member-rule}
::::

#### _parent

```text
thirdparty.pparse.lib.node_context.NodeContext._parent
```

[]{#thirdparty.pparse.lib.node_context.NodeContext._start}

:::: {.member-rule}
::::

#### _start

```text
thirdparty.pparse.lib.node_context.NodeContext._start
```

[]{#thirdparty.pparse.lib.node_context.NodeContext._end}

:::: {.member-rule}
::::

#### _end

```text
thirdparty.pparse.lib.node_context.NodeContext._end
```

[]{#thirdparty.pparse.lib.node_context.NodeContext._parser}

:::: {.member-rule}
::::

#### _parser

```text
thirdparty.pparse.lib.node_context.NodeContext._parser
```

[]{#thirdparty.pparse.lib.node_context.NodeContext._descendants}

:::: {.member-rule}
::::

#### _descendants

```text
thirdparty.pparse.lib.node_context.NodeContext._descendants
```

[]{#thirdparty.pparse.lib.node_context.NodeContext.parent}

:::: {.member-rule}
::::

#### parent

```text
thirdparty.pparse.lib.node_context.NodeContext.parent
```

Return the parent node of this context's node.

**Returns:**

  - `Optional[Node]` - The parent ``Node``, or ``None`` if this is the root node.

[]{#thirdparty.pparse.lib.node_context.NodeContext.parent_ctx}

:::: {.member-rule}
::::

#### parent_ctx

```text
thirdparty.pparse.lib.node_context.NodeContext.parent_ctx
```

Return the ``NodeContext`` of the parent node.

**Returns:**

  - `Optional[NodeContext]` - The parent's ``NodeContext``, or ``None`` if there is no parent.

[]{#thirdparty.pparse.lib.node_context.NodeContext.reader}

:::: {.member-rule}
::::

#### reader

```text
thirdparty.pparse.lib.node_context.NodeContext.reader
```

Return an independent copy of the internal reader.

**Returns:**

  - `Reader` - A duplicated ``Reader`` at the current read position.

[]{#thirdparty.pparse.lib.node_context.NodeContext.parser}

:::: {.member-rule}
::::

#### parser

```text
thirdparty.pparse.lib.node_context.NodeContext.parser
```

Return the parser associated with this context.

**Returns:**

  - `Parser` - The ``Parser`` instance for this node.

[]{#thirdparty.pparse.lib.node_context.NodeContext._init_state}

:::: {.member-rule}
::::

#### _init_state

```text
thirdparty.pparse.lib.node_context.NodeContext._init_state
```

Replace the entire state stack with a single initial state.

**Parameters:**

  - `state` - Type[Any] - A state class (not an instance) to instantiate and push.

[]{#thirdparty.pparse.lib.node_context.NodeContext._init_states}

:::: {.member-rule}
::::

#### _init_states

```text
thirdparty.pparse.lib.node_context.NodeContext._init_states
```

Replace the entire state stack with a sequence of initial states.

States are provided in execution order; the first state in the list
will be on top of the stack (processed first).

**Parameters:**

  - `states` - List[Type[Any]] - A list of state classes to instantiate.

[]{#thirdparty.pparse.lib.node_context.NodeContext._next_state}

:::: {.member-rule}
::::

#### _next_state

```text
thirdparty.pparse.lib.node_context.NodeContext._next_state
```

Replace the top-of-stack state with a new state.

If the stack is empty the new state is appended instead.

**Parameters:**

  - `state` - Type[Any] - A state class (not an instance) to instantiate.

[]{#thirdparty.pparse.lib.node_context.NodeContext._next_states}

:::: {.member-rule}
::::

#### _next_states

```text
thirdparty.pparse.lib.node_context.NodeContext._next_states
```

Replace the top-of-stack state with a sequence of new states.

Equivalent to popping the current top and pushing ``states`` in
execution order.

**Parameters:**

  - `states` - List[Type[Any]] - A list of state classes to instantiate and push.

[]{#thirdparty.pparse.lib.node_context.NodeContext.state}

:::: {.member-rule}
::::

#### state

```text
thirdparty.pparse.lib.node_context.NodeContext.state
```

Return the current (top-of-stack) state instance.

**Returns:**

  - `Any` - The active parser state object.

[]{#thirdparty.pparse.lib.node_context.NodeContext._pop_state}

:::: {.member-rule}
::::

#### _pop_state

```text
thirdparty.pparse.lib.node_context.NodeContext._pop_state
```

Remove and return the top-of-stack state.

The last remaining state can never be popped; use ``state()`` instead.

**Returns:**

  - `Any` - The popped state instance.

**Raises:**

  - `Any` - The popped state instance.

[]{#thirdparty.pparse.lib.node_context.NodeContext.set_remaining}

:::: {.member-rule}
::::

#### set_remaining

```text
thirdparty.pparse.lib.node_context.NodeContext.set_remaining
```

Record the end of this node as ``current_position + length``.

**Parameters:**

  - `length` - int - Number of bytes remaining in the node from the current position.

[]{#thirdparty.pparse.lib.node_context.NodeContext.mark_end}

:::: {.member-rule}
::::

#### mark_end

```text
thirdparty.pparse.lib.node_context.NodeContext.mark_end
```

Record the current position as the end of the node and update its length.

**Parameters:**

  - `node` - Node - The ``Node`` whose length should be set based on the bytes consumed.

[]{#thirdparty.pparse.lib.node_context.NodeContext.mark_field_start}

:::: {.member-rule}
::::

#### mark_field_start

```text
thirdparty.pparse.lib.node_context.NodeContext.mark_field_start
```

Record the current read position as the start of the next field.

[]{#thirdparty.pparse.lib.node_context.NodeContext.field_start}

:::: {.member-rule}
::::

#### field_start

```text
thirdparty.pparse.lib.node_context.NodeContext.field_start
```

Return the byte offset recorded by the most recent ``mark_field_start`` call.

**Returns:**

  - `int` - The saved field-start byte offset.

[]{#thirdparty.pparse.lib.node_context.NodeContext.dup}

:::: {.member-rule}
::::

#### dup

```text
thirdparty.pparse.lib.node_context.NodeContext.dup
```

Return an independent copy of the internal reader.

**Returns:**

  - `Reader` - A duplicated ``Reader`` at the current read position.

[]{#thirdparty.pparse.lib.node_context.NodeContext.tell}

:::: {.member-rule}
::::

#### tell

```text
thirdparty.pparse.lib.node_context.NodeContext.tell
```

Return the current byte offset of the internal reader.

**Returns:**

  - `int` - The current read position.

[]{#thirdparty.pparse.lib.node_context.NodeContext.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.node_context.NodeContext.seek
```

Forward a seek call to the internal reader.

**Parameters:**

  - `*args` - Any - Positional arguments forwarded to ``Reader.seek``.
  - `**kwargs` - Any - Keyword arguments forwarded to ``Reader.seek``.

**Returns:**

  - `Any` - Whatever the underlying ``Reader.seek`` returns.

[]{#thirdparty.pparse.lib.node_context.NodeContext.skip}

:::: {.member-rule}
::::

#### skip

```text
thirdparty.pparse.lib.node_context.NodeContext.skip
```

Forward a skip call to the internal reader.

**Parameters:**

  - `*args` - Any - Positional arguments forwarded to ``Reader.skip``.
  - `**kwargs` - Any - Keyword arguments forwarded to ``Reader.skip``.

**Returns:**

  - `Any` - Whatever the underlying ``Reader.skip`` returns.

[]{#thirdparty.pparse.lib.node_context.NodeContext.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.node_context.NodeContext.peek
```

Forward a peek call to the internal reader.

**Parameters:**

  - `*args` - Any - Positional arguments forwarded to ``Reader.peek``.
  - `**kwargs` - Any - Keyword arguments forwarded to ``Reader.peek``.

**Returns:**

  - `bytes` - Bytes read by the underlying ``Reader.peek``.

[]{#thirdparty.pparse.lib.node_context.NodeContext.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.node_context.NodeContext.read
```

Forward a read call to the internal reader.

**Parameters:**

  - `*args` - Any - Positional arguments forwarded to ``Reader.read``.
  - `**kwargs` - Any - Keyword arguments forwarded to ``Reader.read``.

**Returns:**

  - `bytes` - Bytes read by the underlying ``Reader.read``.

[]{#thirdparty.pparse.lib.node_context.NodeContext.left}

:::: {.member-rule}
::::

#### left

```text
thirdparty.pparse.lib.node_context.NodeContext.left
```

Return bytes remaining between the current position and the range end.

The internal reader must be a ``Range`` for this to work.

**Returns:**

  - `int` - Number of bytes remaining in the range.

**Raises:**

  - `int` - Number of bytes remaining in the range.

[]{#thirdparty.pparse.lib.node.RecursionControl}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.node.RecursionControl

```python
class RecursionControl()
```

docstring will go here

::: {.lefttable}
| Name                                                                            |
| ------------------------------------------------------------------------------- |
| [`MAX_DEPTH`](#thirdparty.pparse.lib.node.RecursionControl.MAX_DEPTH)           |
| [`__init__`](#thirdparty.pparse.lib.node.RecursionControl.__init__)             |
| [`cur_depth`](#thirdparty.pparse.lib.node.RecursionControl.cur_depth)           |
| [`max_seen_depth`](#thirdparty.pparse.lib.node.RecursionControl.max_seen_depth) |
| [`min_depth`](#thirdparty.pparse.lib.node.RecursionControl.min_depth)           |
| [`max_depth`](#thirdparty.pparse.lib.node.RecursionControl.max_depth)           |
| [`cb`](#thirdparty.pparse.lib.node.RecursionControl.cb)                         |
| [`stopped`](#thirdparty.pparse.lib.node.RecursionControl.stopped)               |
| [`increase_depth`](#thirdparty.pparse.lib.node.RecursionControl.increase_depth) |
| [`decrease_depth`](#thirdparty.pparse.lib.node.RecursionControl.decrease_depth) |
| [`current_depth`](#thirdparty.pparse.lib.node.RecursionControl.current_depth)   |
| [`deepest_depth`](#thirdparty.pparse.lib.node.RecursionControl.deepest_depth)   |
:::

[]{#thirdparty.pparse.lib.node.RecursionControl.MAX_DEPTH}

:::: {.member-rule}
::::

#### MAX_DEPTH

```text
thirdparty.pparse.lib.node.RecursionControl.MAX_DEPTH
```

[]{#thirdparty.pparse.lib.node.RecursionControl.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.node.RecursionControl.__init__
```

[]{#thirdparty.pparse.lib.node.RecursionControl.cur_depth}

:::: {.member-rule}
::::

#### cur_depth

```text
thirdparty.pparse.lib.node.RecursionControl.cur_depth
```

[]{#thirdparty.pparse.lib.node.RecursionControl.max_seen_depth}

:::: {.member-rule}
::::

#### max_seen_depth

```text
thirdparty.pparse.lib.node.RecursionControl.max_seen_depth
```

[]{#thirdparty.pparse.lib.node.RecursionControl.min_depth}

:::: {.member-rule}
::::

#### min_depth

```text
thirdparty.pparse.lib.node.RecursionControl.min_depth
```

[]{#thirdparty.pparse.lib.node.RecursionControl.max_depth}

:::: {.member-rule}
::::

#### max_depth

```text
thirdparty.pparse.lib.node.RecursionControl.max_depth
```

[]{#thirdparty.pparse.lib.node.RecursionControl.cb}

:::: {.member-rule}
::::

#### cb

```text
thirdparty.pparse.lib.node.RecursionControl.cb
```

[]{#thirdparty.pparse.lib.node.RecursionControl.stopped}

:::: {.member-rule}
::::

#### stopped

```text
thirdparty.pparse.lib.node.RecursionControl.stopped
```

Return whether recursion should halt at ``node``.

**Parameters:**

  - `node` - Node - The ``Node`` that is about to be loaded.

**Returns:**

  - `bool` - the min/max window and no callback is configured.

[]{#thirdparty.pparse.lib.node.RecursionControl.increase_depth}

:::: {.member-rule}
::::

#### increase_depth

```text
thirdparty.pparse.lib.node.RecursionControl.increase_depth
```

Increment the current recursion depth and update the high-water mark.

**Parameters:**

  - `amount` - int - Number of levels to increase by.

[]{#thirdparty.pparse.lib.node.RecursionControl.decrease_depth}

:::: {.member-rule}
::::

#### decrease_depth

```text
thirdparty.pparse.lib.node.RecursionControl.decrease_depth
```

Decrement the current recursion depth.

**Parameters:**

  - `amount` - int - Number of levels to decrease by.

[]{#thirdparty.pparse.lib.node.RecursionControl.current_depth}

:::: {.member-rule}
::::

#### current_depth

```text
thirdparty.pparse.lib.node.RecursionControl.current_depth
```

Return the current recursion depth.

**Returns:**

  - `int` - The number of ``load`` calls currently on the call stack.

[]{#thirdparty.pparse.lib.node.RecursionControl.deepest_depth}

:::: {.member-rule}
::::

#### deepest_depth

```text
thirdparty.pparse.lib.node.RecursionControl.deepest_depth
```

Return the maximum recursion depth seen since this instance was created.

**Returns:**

  - `int` - The high-water mark for recursion depth.

[]{#thirdparty.pparse.lib.node.Node}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.node.Node

```python
class Node()
```

docstring will go here

::: {.lefttable}
| Name                                                        |
| ----------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.node.Node.__init__)     |
| [`_reader`](#thirdparty.pparse.lib.node.Node._reader)       |
| [`_ctx`](#thirdparty.pparse.lib.node.Node._ctx)             |
| [`_value`](#thirdparty.pparse.lib.node.Node._value)         |
| [`value`](#thirdparty.pparse.lib.node.Node.value)           |
| [`ctx`](#thirdparty.pparse.lib.node.Node.ctx)               |
| [`clear_ctx`](#thirdparty.pparse.lib.node.Node.clear_ctx)   |
| [`tell`](#thirdparty.pparse.lib.node.Node.tell)             |
| [`set_length`](#thirdparty.pparse.lib.node.Node.set_length) |
| [`length`](#thirdparty.pparse.lib.node.Node.length)         |
| [`load`](#thirdparty.pparse.lib.node.Node.load)             |
| [`unload`](#thirdparty.pparse.lib.node.Node.unload)         |
| [`dump`](#thirdparty.pparse.lib.node.Node.dump)             |
| [`from_xml`](#thirdparty.pparse.lib.node.Node.from_xml)     |
:::

[]{#thirdparty.pparse.lib.node.Node.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.node.Node.__init__
```

[]{#thirdparty.pparse.lib.node.Node._reader}

:::: {.member-rule}
::::

#### _reader

```text
thirdparty.pparse.lib.node.Node._reader
```

weird situation here where I don't want to double bind Node and 
NodeContext, but they are indirectly double bound by NodeContext
knowing parent of node. This is intentional so that we can throw
away unnecessary references to parents later.

[]{#thirdparty.pparse.lib.node.Node._ctx}

:::: {.member-rule}
::::

#### _ctx

```text
thirdparty.pparse.lib.node.Node._ctx
```

[]{#thirdparty.pparse.lib.node.Node._value}

:::: {.member-rule}
::::

#### _value

```text
thirdparty.pparse.lib.node.Node._value
```

[]{#thirdparty.pparse.lib.node.Node.value}

:::: {.member-rule}
::::

#### value

```text
thirdparty.pparse.lib.node.Node.value
```

Return the parsed value, triggering ``load()`` if not yet parsed.

**Returns:**

  - `Any` - The node's parsed value (dict, list, scalar, or nested ``Node``).

[]{#thirdparty.pparse.lib.node.Node.ctx}

:::: {.member-rule}
::::

#### ctx

```text
thirdparty.pparse.lib.node.Node.ctx
```

Return the ``NodeContext`` for this node.

**Returns:**

  - `NodeContext` - The ``NodeContext`` holding the parser state for this node.

[]{#thirdparty.pparse.lib.node.Node.clear_ctx}

:::: {.member-rule}
::::

#### clear_ctx

```text
thirdparty.pparse.lib.node.Node.clear_ctx
```

Release the ``NodeContext`` to free memory.

Should only be called once the node's value is fully loaded and
re-parsing is no longer needed or acceptably thrown away for restoration
of memory.

**Returns:**

  - `Node` - ``self``, to allow chaining.

[]{#thirdparty.pparse.lib.node.Node.tell}

:::: {.member-rule}
::::

#### tell

```text
thirdparty.pparse.lib.node.Node.tell
```

Return the absolute byte offset of the start of this node's data.

**Returns:**

  - `int` - The node's start position within the data source.

[]{#thirdparty.pparse.lib.node.Node.set_length}

:::: {.member-rule}
::::

#### set_length

```text
thirdparty.pparse.lib.node.Node.set_length
```

Replace the internal reader with a ``Range`` of ``length`` bytes.

**Parameters:**

  - `length` - int - Number of bytes in this node's data region.

**Returns:**

  - `Node` - ``self``, to allow chaining.

[]{#thirdparty.pparse.lib.node.Node.length}

:::: {.member-rule}
::::

#### length

```text
thirdparty.pparse.lib.node.Node.length
```

Return the byte length of this node's data region.

**Returns:**

  - `int` - The number of bytes in the node's ``Range``.

[]{#thirdparty.pparse.lib.node.Node.load}

:::: {.member-rule}
::::

#### load

```text
thirdparty.pparse.lib.node.Node.load
```

Parse this node's data region and populate ``_value``.

Drives the parser state machine until ``EndOfNodeException`` (success),
``EndOfDataException`` (propagated), or ``UnsupportedFormatException``
(propagated) is raised.  Whenever the state machine registers
descendant nodes they are immediately loaded in depth-first order,
subject to ``recursion`` constraints.

``load()`` manages ``RecursionControl`` lifetime, enabling different
behaviors per call and managing recursion depth relative to the
current node.

CAUTION: Recursion control is a mechanism that allows parsers to delegate recursion
decisions to the caller or user of the API. It does not enforce or provide
tight controls over the parts of the Node tree that get processed.

RULE: load() should handle the recursive behavior, not the parser state.

RULE: Leaky abstraction when recursion policy stored in Node or NodeContext!

NOTE: json parsing is weird because you have to recurse every time. Since I
implemented JSON first, I believe its driving some of the anti-patterns in pparse.
If we want to save on memory for JSON, we could theoretically parse and allocate
nodes and as we complete branches of a depth first parse we deallocated.
I naturally want to do this for recursive=False, but since recursive=False
is default, it makes it feel like unnecessary thrashing of the CPU.

**Parameters:**

  - `recursion` - Optional[RecursionControl] - An optional ``RecursionControl`` that limits how deeply
the parse tree is expanded.  When ``None`` all reachable
descendants are loaded eagerly.

**Returns:**

  - `Optional[Node]` - loading before any state was run.

**Raises:**

  - `Optional[Node]` - loading before any state was run.

[]{#thirdparty.pparse.lib.node.Node.unload}

:::: {.member-rule}
::::

#### unload

```text
thirdparty.pparse.lib.node.Node.unload
```

[]{#thirdparty.pparse.lib.node.Node.dump}

:::: {.member-rule}
::::

#### dump

```text
thirdparty.pparse.lib.node.Node.dump
```

Render this node to the given dumper for debugging or serialization.

**Parameters:**

  - `depth` - int - Current indentation depth passed to the dumper.
  - `step` - int - Number of spaces per indentation level.
  - `dumper` - Any - A ``Dumper`` instance; ``Dumper.default()`` is used when
this is ``None``.

[]{#thirdparty.pparse.lib.node.Node.from_xml}

:::: {.member-rule}
::::

#### from_xml

```text
thirdparty.pparse.lib.node.Node.from_xml
```

Reconstruct a ``Node`` from a ``<node />`` XML element.

Reads parser, context, and value information from the XML, resolves
the appropriate parser via a built-in registry, and recursively
deserializes any nested ``<node />`` values.

**Parameters:**

  - `src_xml` - Any - A ``<node />`` XML element or string.
  - `ctx_ref` - Any - A ``ContextRef`` carrying the parent result reference,
optional context XML, and resolved parser.

**Returns:**

  - `Optional[Node]` - ``<value />`` element (i.e. is left in the unloaded state).

**Raises:**

  - `Optional[Node]` - ``<value />`` element (i.e. is left in the unloaded state).

[]{#thirdparty.pparse.lib.reader.Reader}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.reader.Reader

```python
class Reader()
```

docstring will go here

::: {.lefttable}
| Name                                                |
| --------------------------------------------------- |
| [`dup`](#thirdparty.pparse.lib.reader.Reader.dup)   |
| [`tell`](#thirdparty.pparse.lib.reader.Reader.tell) |
| [`seek`](#thirdparty.pparse.lib.reader.Reader.seek) |
| [`skip`](#thirdparty.pparse.lib.reader.Reader.skip) |
| [`peek`](#thirdparty.pparse.lib.reader.Reader.peek) |
| [`read`](#thirdparty.pparse.lib.reader.Reader.read) |
:::

[]{#thirdparty.pparse.lib.reader.Reader.dup}

:::: {.member-rule}
::::

#### dup

```text
thirdparty.pparse.lib.reader.Reader.dup
```

Return an independent copy of this reader positioned at the same offset.

**Returns:**

  - `Reader` - A new ``Reader`` at the current position.

[]{#thirdparty.pparse.lib.reader.Reader.tell}

:::: {.member-rule}
::::

#### tell

```text
thirdparty.pparse.lib.reader.Reader.tell
```

Return the current absolute byte offset within the data source.

**Returns:**

  - `int` - The current read position as a byte offset.

[]{#thirdparty.pparse.lib.reader.Reader.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.reader.Reader.seek
```

Move the read position to an absolute byte offset.

**Parameters:**

  - `offset` - int - Target byte offset within the data source.

**Returns:**

  - `Any` - Implementation-defined; typically the resolved offset.

[]{#thirdparty.pparse.lib.reader.Reader.skip}

:::: {.member-rule}
::::

#### skip

```text
thirdparty.pparse.lib.reader.Reader.skip
```

Advance the read position by ``length`` bytes without reading.

**Parameters:**

  - `length` - int - Number of bytes to skip forward.

**Returns:**

  - `Any` - Implementation-defined; typically the new offset.

[]{#thirdparty.pparse.lib.reader.Reader.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.reader.Reader.peek
```

Read ``length`` bytes starting at the current position without advancing.

**Parameters:**

  - `length` - int - Number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.reader.Reader.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.reader.Reader.read
```

Read ``length`` bytes and advance the read position accordingly.

**Parameters:**

  - `length` - int - Number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.reader.Cursor}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.reader.Cursor

```python
class Cursor(Reader)
```

docstring will go here

::: {.lefttable}
| Name                                                        |
| ----------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.reader.Cursor.__init__) |
| [`_data`](#thirdparty.pparse.lib.reader.Cursor._data)       |
| [`_offset`](#thirdparty.pparse.lib.reader.Cursor._offset)   |
| [`cursor`](#thirdparty.pparse.lib.reader.Cursor.cursor)     |
| [`dup`](#thirdparty.pparse.lib.reader.Cursor.dup)           |
| [`tell`](#thirdparty.pparse.lib.reader.Cursor.tell)         |
| [`seek`](#thirdparty.pparse.lib.reader.Cursor.seek)         |
| [`skip`](#thirdparty.pparse.lib.reader.Cursor.skip)         |
| [`peek`](#thirdparty.pparse.lib.reader.Cursor.peek)         |
| [`read`](#thirdparty.pparse.lib.reader.Cursor.read)         |
:::

[]{#thirdparty.pparse.lib.reader.Cursor.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.reader.Cursor.__init__
```

[]{#thirdparty.pparse.lib.reader.Cursor._data}

:::: {.member-rule}
::::

#### _data

```text
thirdparty.pparse.lib.reader.Cursor._data
```

[]{#thirdparty.pparse.lib.reader.Cursor._offset}

:::: {.member-rule}
::::

#### _offset

```text
thirdparty.pparse.lib.reader.Cursor._offset
```

[]{#thirdparty.pparse.lib.reader.Cursor.cursor}

:::: {.member-rule}
::::

#### cursor

```text
thirdparty.pparse.lib.reader.Cursor.cursor
```

Return this cursor itself. (Maintains ignorance about Cursor vs Range.)

**Returns:**

  - `Cursor` - ``self``.

[]{#thirdparty.pparse.lib.reader.Cursor.dup}

:::: {.member-rule}
::::

#### dup

```text
thirdparty.pparse.lib.reader.Cursor.dup
```

Return a new ``Cursor`` at the current position by calling ``Data.open``.

**Returns:**

  - `Cursor` - A new ``Cursor`` positioned at the current offset.

[]{#thirdparty.pparse.lib.reader.Cursor.tell}

:::: {.member-rule}
::::

#### tell

```text
thirdparty.pparse.lib.reader.Cursor.tell
```

Return the current byte offset within the ``Data`` source.

**Returns:**

  - `int` - The current read position.

[]{#thirdparty.pparse.lib.reader.Cursor.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.reader.Cursor.seek
```

Move this cursor to an absolute byte offset.

**Parameters:**

  - `offset` - int - Target byte offset.

**Returns:**

  - `Any` - The value returned by the underlying ``Data.seek`` call.

[]{#thirdparty.pparse.lib.reader.Cursor.skip}

:::: {.member-rule}
::::

#### skip

```text
thirdparty.pparse.lib.reader.Cursor.skip
```

Advance this cursor by ``length`` bytes without reading any data.

**Parameters:**

  - `length` - int - Number of bytes to skip forward.

**Returns:**

  - `Any` - The value returned by the underlying ``Data.seek`` call.

[]{#thirdparty.pparse.lib.reader.Cursor.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.reader.Cursor.peek
```

Read ``length`` bytes at the current position without advancing the cursor.

**Parameters:**

  - `length` - int - Number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes starting at the current offset.

[]{#thirdparty.pparse.lib.reader.Cursor.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.reader.Cursor.read
```

Read ``length`` bytes.

**Parameters:**

  - `length` - int - Max number of bytes to read.
  - `mode` - Any - Optional read mode hint passed to the backing data source.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.reader.Range}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.reader.Range

```python
class Range(Reader)
```

docstring will go here

::: {.lefttable}
| Name                                                                   |
| ---------------------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.reader.Range.__init__)             |
| [`_start_cursor`](#thirdparty.pparse.lib.reader.Range._start_cursor)   |
| [`_init`](#thirdparty.pparse.lib.reader.Range._init)                   |
| [`cursor`](#thirdparty.pparse.lib.reader.Range.cursor)                 |
| [`dup`](#thirdparty.pparse.lib.reader.Range.dup)                       |
| [`truncate`](#thirdparty.pparse.lib.reader.Range.truncate)             |
| [`length`](#thirdparty.pparse.lib.reader.Range.length)                 |
| [`left`](#thirdparty.pparse.lib.reader.Range.left)                     |
| [`valid_offset`](#thirdparty.pparse.lib.reader.Range.valid_offset)     |
| [`tell`](#thirdparty.pparse.lib.reader.Range.tell)                     |
| [`seek`](#thirdparty.pparse.lib.reader.Range.seek)                     |
| [`_adjust_length`](#thirdparty.pparse.lib.reader.Range._adjust_length) |
| [`skip`](#thirdparty.pparse.lib.reader.Range.skip)                     |
| [`peek`](#thirdparty.pparse.lib.reader.Range.peek)                     |
| [`read`](#thirdparty.pparse.lib.reader.Range.read)                     |
:::

[]{#thirdparty.pparse.lib.reader.Range.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.reader.Range.__init__
```

[]{#thirdparty.pparse.lib.reader.Range._start_cursor}

:::: {.member-rule}
::::

#### _start_cursor

```text
thirdparty.pparse.lib.reader.Range._start_cursor
```

[]{#thirdparty.pparse.lib.reader.Range._init}

:::: {.member-rule}
::::

#### _init

```text
thirdparty.pparse.lib.reader.Range._init
```

Initialise internal state from scalar offsets rather than a cursor object.

**Parameters:**

  - `start_offset` - int - Absolute byte offset at which the range begins.
  - `length` - int - Number of bytes in the range.
  - `current_offset` - int - Absolute byte offset for the initial read position.
If negative the read position is set to ``start_offset``.

**Raises:**


[]{#thirdparty.pparse.lib.reader.Range.cursor}

:::: {.member-rule}
::::

#### cursor

```text
thirdparty.pparse.lib.reader.Range.cursor
```

Return an independent copy of the internal read cursor.

**Returns:**

  - `Cursor` - A duplicated ``Cursor`` at the current read position.

[]{#thirdparty.pparse.lib.reader.Range.dup}

:::: {.member-rule}
::::

#### dup

```text
thirdparty.pparse.lib.reader.Range.dup
```

Return an independent copy of this range at the same read position.

**Returns:**

  - `Range` - A new ``Range`` with the same bounds and current offset.

[]{#thirdparty.pparse.lib.reader.Range.truncate}

:::: {.member-rule}
::::

#### truncate

```text
thirdparty.pparse.lib.reader.Range.truncate
```

Shorten the range to ``new_length`` bytes.

**Parameters:**

  - `new_length` - int - The replacement length.  Must be <= the current length,
and the current read position must not fall inside the truncated
region.

**Returns:**

  - `Range` - ``self``, to allow chaining.

**Raises:**

  - `Range` - ``self``, to allow chaining.

[]{#thirdparty.pparse.lib.reader.Range.length}

:::: {.member-rule}
::::

#### length

```text
thirdparty.pparse.lib.reader.Range.length
```

Return the total number of bytes in this range.

**Returns:**

  - `int` - The range length in bytes.

[]{#thirdparty.pparse.lib.reader.Range.left}

:::: {.member-rule}
::::

#### left

```text
thirdparty.pparse.lib.reader.Range.left
```

Return the number of bytes remaining between the cursor and the end of the range.

**Returns:**

  - `int` - Bytes available to read from the current position.

[]{#thirdparty.pparse.lib.reader.Range.valid_offset}

:::: {.member-rule}
::::

#### valid_offset

```text
thirdparty.pparse.lib.reader.Range.valid_offset
```

Return whether ``offset`` falls within ``[start, end]`` (inclusive).

**Parameters:**

  - `offset` - int - Absolute byte offset to test.

**Returns:**

  - `bool` - ``True`` if the offset is within bounds, ``False`` otherwise.

[]{#thirdparty.pparse.lib.reader.Range.tell}

:::: {.member-rule}
::::

#### tell

```text
thirdparty.pparse.lib.reader.Range.tell
```

Return the current absolute byte offset of the internal cursor.

**Returns:**

  - `int` - The current read position.

[]{#thirdparty.pparse.lib.reader.Range.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.reader.Range.seek
```

Move the read position to an absolute offset, clamped to the range bounds.

If ``offset`` is outside ``[start, end]`` it is clamped to the nearest
boundary rather than raising an error.

**Parameters:**

  - `offset` - int - Target absolute byte offset.

**Returns:**

  - `int` - The (possibly clamped) offset that was actually ``seek``ed.

[]{#thirdparty.pparse.lib.reader.Range._adjust_length}

:::: {.member-rule}
::::

#### _adjust_length

```text
thirdparty.pparse.lib.reader.Range._adjust_length
```

Clamp ``length`` so that reading it would not exceed the range end.

**Parameters:**

  - `length` - int - Requested number of bytes.

**Returns:**

  - `int` - The adjusted (clamped) byte count, guaranteed to be >= 0.

[]{#thirdparty.pparse.lib.reader.Range.skip}

:::: {.member-rule}
::::

#### skip

```text
thirdparty.pparse.lib.reader.Range.skip
```

Advance the read position by up to ``length`` bytes, clamped to bounds.

**Parameters:**

  - `length` - int - Max number of bytes to skip forward.

**Returns:**

  - `Any` - The value returned by the underlying cursor's skip call.

[]{#thirdparty.pparse.lib.reader.Range.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.reader.Range.peek
```

Read up to ``length`` bytes at the current position without advancing.

The byte count is clamped so it does not exceed the range boundary.

**Parameters:**

  - `length` - int - Max number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.reader.Range.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.reader.Range.read
```

Read up to ``length`` bytes.

The byte count is clamped so it does not exceed the range boundary.

**Parameters:**

  - `length` - int - Max number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.data.Data}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.data.Data

```python
class Data()
```

docstring will go here

::: {.lefttable}
| Name                                            |
| ----------------------------------------------- |
| [`open`](#thirdparty.pparse.lib.data.Data.open) |
| [`peek`](#thirdparty.pparse.lib.data.Data.peek) |
| [`seek`](#thirdparty.pparse.lib.data.Data.seek) |
| [`read`](#thirdparty.pparse.lib.data.Data.read) |
:::

[]{#thirdparty.pparse.lib.data.Data.open}

:::: {.member-rule}
::::

#### open

```text
thirdparty.pparse.lib.data.Data.open
```

Return a new ``Cursor`` into this data source at ``offset``.

**Parameters:**

  - `offset` - int - Initial byte offset for the cursor.

**Returns:**

  - `Cursor` - A ``Cursor`` positioned at ``offset``.

[]{#thirdparty.pparse.lib.data.Data.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.data.Data.peek
```

Read ``length`` bytes at the cursor's position without advancing offset.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

**Raises:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.data.Data.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.data.Data.seek
```

Update the underlying file descriptor or upstream libraries internal offset.

The base implementation is a no-op that returns the cursor's current
position.  Subclasses that maintain their own file pointer should
override ``seek``.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` whose offset should be applied.

**Returns:**

  - `int` - The cursor's current byte offset.

[]{#thirdparty.pparse.lib.data.Data.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.data.Data.read
```

Read ``length`` bytes.

The base implementation uses ``peek``.  Subclasses that can perform a more
efficient read-and-seek in a single syscall should override ``read``.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Max number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.data.HttpCachedData}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.data.HttpCachedData

```python
class HttpCachedData(Data)
```

docstring will go here

::: {.lefttable}
| Name                                                                              |
| --------------------------------------------------------------------------------- |
| [`CHUNK_SIZE`](#thirdparty.pparse.lib.data.HttpCachedData.CHUNK_SIZE)             |
| [`MAX_CHUNKS`](#thirdparty.pparse.lib.data.HttpCachedData.MAX_CHUNKS)             |
| [`__init__`](#thirdparty.pparse.lib.data.HttpCachedData.__init__)                 |
| [`_session`](#thirdparty.pparse.lib.data.HttpCachedData._session)                 |
| [`length`](#thirdparty.pparse.lib.data.HttpCachedData.length)                     |
| [`_supports_ranges`](#thirdparty.pparse.lib.data.HttpCachedData._supports_ranges) |
| [`httpdata`](#thirdparty.pparse.lib.data.HttpCachedData.httpdata)                 |
| [`peek`](#thirdparty.pparse.lib.data.HttpCachedData.peek)                         |
:::

[]{#thirdparty.pparse.lib.data.HttpCachedData.CHUNK_SIZE}

:::: {.member-rule}
::::

#### CHUNK_SIZE

```text
thirdparty.pparse.lib.data.HttpCachedData.CHUNK_SIZE
```

[]{#thirdparty.pparse.lib.data.HttpCachedData.MAX_CHUNKS}

:::: {.member-rule}
::::

#### MAX_CHUNKS

```text
thirdparty.pparse.lib.data.HttpCachedData.MAX_CHUNKS
```

[]{#thirdparty.pparse.lib.data.HttpCachedData.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.data.HttpCachedData.__init__
```

[]{#thirdparty.pparse.lib.data.HttpCachedData._session}

:::: {.member-rule}
::::

#### _session

```text
thirdparty.pparse.lib.data.HttpCachedData._session
```

[]{#thirdparty.pparse.lib.data.HttpCachedData.length}

:::: {.member-rule}
::::

#### length

```text
thirdparty.pparse.lib.data.HttpCachedData.length
```

[]{#thirdparty.pparse.lib.data.HttpCachedData._supports_ranges}

:::: {.member-rule}
::::

#### _supports_ranges

```text
thirdparty.pparse.lib.data.HttpCachedData._supports_ranges
```

[]{#thirdparty.pparse.lib.data.HttpCachedData.httpdata}

:::: {.member-rule}
::::

#### httpdata

```text
thirdparty.pparse.lib.data.HttpCachedData.httpdata
```

[]{#thirdparty.pparse.lib.data.HttpCachedData.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.data.HttpCachedData.peek
```

Read ``length`` bytes at the cursor's position via the chunk cache.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.data.FileData}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.data.FileData

```python
class FileData(Data)
```

docstring will go here

::: {.lefttable}
| Name                                                        |
| ----------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.data.FileData.__init__) |
| [`_path`](#thirdparty.pparse.lib.data.FileData._path)       |
| [`length`](#thirdparty.pparse.lib.data.FileData.length)     |
| [`_fobj`](#thirdparty.pparse.lib.data.FileData._fobj)       |
| [`peek`](#thirdparty.pparse.lib.data.FileData.peek)         |
| [`seek`](#thirdparty.pparse.lib.data.FileData.seek)         |
| [`read`](#thirdparty.pparse.lib.data.FileData.read)         |
| [`from_xml`](#thirdparty.pparse.lib.data.FileData.from_xml) |
| [`to_xml`](#thirdparty.pparse.lib.data.FileData.to_xml)     |
:::

[]{#thirdparty.pparse.lib.data.FileData.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.data.FileData.__init__
```

[]{#thirdparty.pparse.lib.data.FileData._path}

:::: {.member-rule}
::::

#### _path

```text
thirdparty.pparse.lib.data.FileData._path
```

[]{#thirdparty.pparse.lib.data.FileData.length}

:::: {.member-rule}
::::

#### length

```text
thirdparty.pparse.lib.data.FileData.length
```

[]{#thirdparty.pparse.lib.data.FileData._fobj}

:::: {.member-rule}
::::

#### _fobj

```text
thirdparty.pparse.lib.data.FileData._fobj
```

[]{#thirdparty.pparse.lib.data.FileData.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.data.FileData.peek
```

Seek the file to the cursor's position and read ``length`` bytes.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.data.FileData.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.data.FileData.seek
```

Update file descriptor to the cursor's byte offset.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` whose offset to apply.

**Returns:**

  - `int` - The cursor's current byte offset.

[]{#thirdparty.pparse.lib.data.FileData.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.data.FileData.read
```

Read ``length`` bytes.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Max number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.data.FileData.from_xml}

:::: {.member-rule}
::::

#### from_xml

```text
thirdparty.pparse.lib.data.FileData.from_xml
```

Deserialize a ``FileData`` from a ``<datasource />`` XML element.

Reads the ``posix_path`` (or ``windows_path``) from the element's
``<extra />`` child and attempts to validate the file is unchanges by
comparing the on-disk file length to the XML recorded length.

**Parameters:**

  - `xml_src` - Any - An XML element or string for the ``<datasource />`` node.

**Returns:**

  - `FileData` - A ``FileData`` instance opened on the described file.

**Raises:**

  - `FileData` - A ``FileData`` instance opened on the described file.

[]{#thirdparty.pparse.lib.data.FileData.to_xml}

:::: {.member-rule}
::::

#### to_xml

```text
thirdparty.pparse.lib.data.FileData.to_xml
```

[]{#thirdparty.pparse.lib.data.FileMmapData}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.data.FileMmapData

```python
class FileMmapData(Data)
```

docstring will go here

::: {.lefttable}
| Name                                                                    |
| ----------------------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.data.FileMmapData.__init__)         |
| [`_path`](#thirdparty.pparse.lib.data.FileMmapData._path)               |
| [`length`](#thirdparty.pparse.lib.data.FileMmapData.length)             |
| [`_fobj`](#thirdparty.pparse.lib.data.FileMmapData._fobj)               |
| [`_mmap`](#thirdparty.pparse.lib.data.FileMmapData._mmap)               |
| [`_mem`](#thirdparty.pparse.lib.data.FileMmapData._mem)                 |
| [`_load_length`](#thirdparty.pparse.lib.data.FileMmapData._load_length) |
| [`peek`](#thirdparty.pparse.lib.data.FileMmapData.peek)                 |
| [`seek`](#thirdparty.pparse.lib.data.FileMmapData.seek)                 |
| [`read`](#thirdparty.pparse.lib.data.FileMmapData.read)                 |
:::

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.data.FileMmapData.__init__
```

[]{#thirdparty.pparse.lib.data.FileMmapData._path}

:::: {.member-rule}
::::

#### _path

```text
thirdparty.pparse.lib.data.FileMmapData._path
```

[]{#thirdparty.pparse.lib.data.FileMmapData.length}

:::: {.member-rule}
::::

#### length

```text
thirdparty.pparse.lib.data.FileMmapData.length
```

[]{#thirdparty.pparse.lib.data.FileMmapData._fobj}

:::: {.member-rule}
::::

#### _fobj

```text
thirdparty.pparse.lib.data.FileMmapData._fobj
```

[]{#thirdparty.pparse.lib.data.FileMmapData._mmap}

:::: {.member-rule}
::::

#### _mmap

```text
thirdparty.pparse.lib.data.FileMmapData._mmap
```

[]{#thirdparty.pparse.lib.data.FileMmapData._mem}

:::: {.member-rule}
::::

#### _mem

```text
thirdparty.pparse.lib.data.FileMmapData._mem
```

[]{#thirdparty.pparse.lib.data.FileMmapData._load_length}

:::: {.member-rule}
::::

#### _load_length

```text
thirdparty.pparse.lib.data.FileMmapData._load_length
```

Populate ``self.length`` from the file's ``stat`` size.

Note: The recorded size is only accurate at open time; it will not
reflect subsequent truncations or appends.

[]{#thirdparty.pparse.lib.data.FileMmapData.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.data.FileMmapData.peek
```

Return a zero-copy ``memoryview`` slice at the cursor's position.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Number of bytes to expose.

**Returns:**

  - `memoryview` - A ``memoryview`` over the requested byte range.

[]{#thirdparty.pparse.lib.data.FileMmapData.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.data.FileMmapData.seek
```

No-op seek — mmap access is always random, so no pointer sync is needed.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` whose position to return.

**Returns:**

  - `int` - The cursor's current byte offset.

[]{#thirdparty.pparse.lib.data.FileMmapData.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.data.FileMmapData.read
```

Return a zero-copy ``memoryview`` slice at the cursor's position.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Number of bytes to expose.
  - `mode` - Any - Unused; present for interface compatibility.

**Returns:**

  - `memoryview` - A ``memoryview`` over the requested byte range.

[]{#thirdparty.pparse.lib.data.FileMmapData.__init__}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.data.FileMmapData.__init__

::: {.lefttable}
| Name                                                                     |
| ------------------------------------------------------------------------ |
| [`mmap`](#thirdparty.pparse.lib.data.FileMmapData.__init__.mmap)         |
| [`has_mmap`](#thirdparty.pparse.lib.data.FileMmapData.__init__.has_mmap) |
:::

[]{#thirdparty.pparse.lib.data.FileMmapData.__init__.mmap}

:::: {.member-rule}
::::

#### mmap

```text
thirdparty.pparse.lib.data.FileMmapData.__init__.mmap
```

[]{#thirdparty.pparse.lib.data.FileMmapData.__init__.has_mmap}

:::: {.member-rule}
::::

#### has_mmap

```text
thirdparty.pparse.lib.data.FileMmapData.__init__.has_mmap
```

[]{#thirdparty.pparse.lib.data.BytesIoData}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.data.BytesIoData

```python
class BytesIoData(Data)
```

docstring will go here

::: {.lefttable}
| Name                                                                   |
| ---------------------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.data.BytesIoData.__init__)         |
| [`_bytes_io`](#thirdparty.pparse.lib.data.BytesIoData._bytes_io)       |
| [`length`](#thirdparty.pparse.lib.data.BytesIoData.length)             |
| [`_load_length`](#thirdparty.pparse.lib.data.BytesIoData._load_length) |
| [`open`](#thirdparty.pparse.lib.data.BytesIoData.open)                 |
| [`peek`](#thirdparty.pparse.lib.data.BytesIoData.peek)                 |
| [`seek`](#thirdparty.pparse.lib.data.BytesIoData.seek)                 |
| [`read`](#thirdparty.pparse.lib.data.BytesIoData.read)                 |
:::

[]{#thirdparty.pparse.lib.data.BytesIoData.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.data.BytesIoData.__init__
```

[]{#thirdparty.pparse.lib.data.BytesIoData._bytes_io}

:::: {.member-rule}
::::

#### _bytes_io

```text
thirdparty.pparse.lib.data.BytesIoData._bytes_io
```

[]{#thirdparty.pparse.lib.data.BytesIoData.length}

:::: {.member-rule}
::::

#### length

```text
thirdparty.pparse.lib.data.BytesIoData.length
```

[]{#thirdparty.pparse.lib.data.BytesIoData._load_length}

:::: {.member-rule}
::::

#### _load_length

```text
thirdparty.pparse.lib.data.BytesIoData._load_length
```

``BytesIO`` length is read eagerly in ``__init__``.

[]{#thirdparty.pparse.lib.data.BytesIoData.open}

:::: {.member-rule}
::::

#### open

```text
thirdparty.pparse.lib.data.BytesIoData.open
```

Return a new ``Cursor`` into this buffer at ``offset``.

**Parameters:**

  - `offset` - int - Initial byte offset for the cursor.

**Returns:**

  - `Cursor` - A ``Cursor`` positioned at ``offset``.

[]{#thirdparty.pparse.lib.data.BytesIoData.peek}

:::: {.member-rule}
::::

#### peek

```text
thirdparty.pparse.lib.data.BytesIoData.peek
```

Seek the buffer to the cursor's position and read ``length`` bytes.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.data.BytesIoData.seek}

:::: {.member-rule}
::::

#### seek

```text
thirdparty.pparse.lib.data.BytesIoData.seek
```

Update ``BytesIO`` internal position to the cursor's byte offset.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` whose offset to apply.

**Returns:**

  - `int` - The cursor's current byte offset.

[]{#thirdparty.pparse.lib.data.BytesIoData.read}

:::: {.member-rule}
::::

#### read

```text
thirdparty.pparse.lib.data.BytesIoData.read
```

Read ``length`` bytes.

**Parameters:**

  - `cursor` - Cursor - The ``Cursor`` indicating where to read from.
  - `length` - int - Max number of bytes to read.

**Returns:**

  - `bytes` - Up to ``length`` bytes of data.

[]{#thirdparty.pparse.lib.extraction.Extraction}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.extraction.Extraction

```python
class Extraction()
```

docstring will go here

::: {.lefttable}
| Name                                                                                |
| ----------------------------------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.extraction.Extraction.__init__)                 |
| [`_source`](#thirdparty.pparse.lib.extraction.Extraction._source)                   |
| [`_name`](#thirdparty.pparse.lib.extraction.Extraction._name)                       |
| [`_parser`](#thirdparty.pparse.lib.extraction.Extraction._parser)                   |
| [`_result`](#thirdparty.pparse.lib.extraction.Extraction._result)                   |
| [`_extractions`](#thirdparty.pparse.lib.extraction.Extraction._extractions)         |
| [`name`](#thirdparty.pparse.lib.extraction.Extraction.name)                         |
| [`set_name`](#thirdparty.pparse.lib.extraction.Extraction.set_name)                 |
| [`add_result`](#thirdparty.pparse.lib.extraction.Extraction.add_result)             |
| [`add_parser`](#thirdparty.pparse.lib.extraction.Extraction.add_parser)             |
| [`has_parser`](#thirdparty.pparse.lib.extraction.Extraction.has_parser)             |
| [`discover_parsers`](#thirdparty.pparse.lib.extraction.Extraction.discover_parsers) |
| [`open`](#thirdparty.pparse.lib.extraction.Extraction.open)                         |
| [`scan_data`](#thirdparty.pparse.lib.extraction.Extraction.scan_data)               |
| [`from_xml`](#thirdparty.pparse.lib.extraction.Extraction.from_xml)                 |
| [`to_xml`](#thirdparty.pparse.lib.extraction.Extraction.to_xml)                     |
:::

[]{#thirdparty.pparse.lib.extraction.Extraction.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.extraction.Extraction.__init__
```

[]{#thirdparty.pparse.lib.extraction.Extraction._source}

:::: {.member-rule}
::::

#### _source

```text
thirdparty.pparse.lib.extraction.Extraction._source
```

[]{#thirdparty.pparse.lib.extraction.Extraction._name}

:::: {.member-rule}
::::

#### _name

```text
thirdparty.pparse.lib.extraction.Extraction._name
```

[]{#thirdparty.pparse.lib.extraction.Extraction._parser}

:::: {.member-rule}
::::

#### _parser

```text
thirdparty.pparse.lib.extraction.Extraction._parser
```

[]{#thirdparty.pparse.lib.extraction.Extraction._result}

:::: {.member-rule}
::::

#### _result

```text
thirdparty.pparse.lib.extraction.Extraction._result
```

[]{#thirdparty.pparse.lib.extraction.Extraction._extractions}

:::: {.member-rule}
::::

#### _extractions

```text
thirdparty.pparse.lib.extraction.Extraction._extractions
```

[]{#thirdparty.pparse.lib.extraction.Extraction.name}

:::: {.member-rule}
::::

#### name

```text
thirdparty.pparse.lib.extraction.Extraction.name
```

Return the name of this extraction.

**Returns:**

  - `Optional[str]` - The extraction name, or ``None`` if not set.

[]{#thirdparty.pparse.lib.extraction.Extraction.set_name}

:::: {.member-rule}
::::

#### set_name

```text
thirdparty.pparse.lib.extraction.Extraction.set_name
```

Set the name of this extraction.

**Parameters:**

  - `name` - str - The new name.

**Returns:**

  - `Extraction` - ``self``, to allow chaining.

[]{#thirdparty.pparse.lib.extraction.Extraction.add_result}

:::: {.member-rule}
::::

#### add_result

```text
thirdparty.pparse.lib.extraction.Extraction.add_result
```

Register a root node against a result slot.

**Parameters:**

  - `id` - Any - The result identifier (typically a string key).
  - `root_node` - Optional[Node] - The root ``Node`` of the parse result, or ``None`` if
the result has not been populated yet.

[]{#thirdparty.pparse.lib.extraction.Extraction.add_parser}

:::: {.member-rule}
::::

#### add_parser

```text
thirdparty.pparse.lib.extraction.Extraction.add_parser
```

Register a parser under a string identifier.

**Parameters:**

  - `id` - str - A unique string key for this parser.
  - `parser` - Optional[Parser] - The ``Parser`` instance to register.

[]{#thirdparty.pparse.lib.extraction.Extraction.has_parser}

:::: {.member-rule}
::::

#### has_parser

```text
thirdparty.pparse.lib.extraction.Extraction.has_parser
```

Return whether a parser with the given ID has been registered.

**Parameters:**

  - `parser_id` - str - The parser identifier to check.

**Returns:**

  - `bool` - ``True`` if a parser with that ID exists, ``False`` otherwise.

[]{#thirdparty.pparse.lib.extraction.Extraction.discover_parsers}

:::: {.member-rule}
::::

#### discover_parsers

```text
thirdparty.pparse.lib.extraction.Extraction.discover_parsers
```

Auto-detect applicable parsers from a registry and register them.

For each parser in ``parser_registry`` that has not already been
registered, the method tries ``match_extension`` on the extraction
name and then ``match_magic`` on the data bytes.  The first matching
parser is instantiated and registered.

**Parameters:**

  - `parser_registry` - Dict[str, Any] - A mapping of parser ID strings to parser classes.

**Returns:**

  - `Extraction` - ``self``, to allow chaining.

[]{#thirdparty.pparse.lib.extraction.Extraction.open}

:::: {.member-rule}
::::

#### open

```text
thirdparty.pparse.lib.extraction.Extraction.open
```

Return a fresh ``Reader`` positioned at the start of the ``Extraction``'s data.

**Returns:**

  - `Reader` - A ``Reader`` ready for use at offset 0.

**Raises:**

  - `Reader` - A ``Reader`` ready for use at offset 0.

[]{#thirdparty.pparse.lib.extraction.Extraction.scan_data}

:::: {.member-rule}
::::

#### scan_data

```text
thirdparty.pparse.lib.extraction.Extraction.scan_data
```

Trigger all registered parsers to scan the full data source.

Note: Legacy/Unused

**Returns:**

  - `Extraction` - ``self``, to allow chaining.

[]{#thirdparty.pparse.lib.extraction.Extraction.from_xml}

:::: {.member-rule}
::::

#### from_xml

```text
thirdparty.pparse.lib.extraction.Extraction.from_xml
```

Load and resume ``Extraction`` from state in XML.

**Parameters:**

  - `xml_src` - Any - An XML element or string describing the extraction.
  - `xml_root` - Any - The root XML context used to resolve references.

**Returns:**

  - `Extraction` - A populated ``Extraction`` instance.

**Raises:**

  - `Extraction` - A populated ``Extraction`` instance.

[]{#thirdparty.pparse.lib.extraction.Extraction.to_xml}

:::: {.member-rule}
::::

#### to_xml

```text
thirdparty.pparse.lib.extraction.Extraction.to_xml
```

Stop and save the current state of ``Extraction`` to XML.

**Returns:**

  - `str` - An XML string representation of this extraction.

**Raises:**

  - `str` - An XML string representation of this extraction.

[]{#thirdparty.pparse.lib.extraction.BytesExtraction}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.extraction.BytesExtraction

```python
class BytesExtraction(Extraction)
```

docstring will go here

::: {.lefttable}
| Name                                                                     |
| ------------------------------------------------------------------------ |
| [`__init__`](#thirdparty.pparse.lib.extraction.BytesExtraction.__init__) |
| [`_reader`](#thirdparty.pparse.lib.extraction.BytesExtraction._reader)   |
| [`open`](#thirdparty.pparse.lib.extraction.BytesExtraction.open)         |
| [`tell`](#thirdparty.pparse.lib.extraction.BytesExtraction.tell)         |
| [`from_xml`](#thirdparty.pparse.lib.extraction.BytesExtraction.from_xml) |
| [`to_xml`](#thirdparty.pparse.lib.extraction.BytesExtraction.to_xml)     |
:::

[]{#thirdparty.pparse.lib.extraction.BytesExtraction.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.extraction.BytesExtraction.__init__
```

[]{#thirdparty.pparse.lib.extraction.BytesExtraction._reader}

:::: {.member-rule}
::::

#### _reader

```text
thirdparty.pparse.lib.extraction.BytesExtraction._reader
```

[]{#thirdparty.pparse.lib.extraction.BytesExtraction.open}

:::: {.member-rule}
::::

#### open

```text
thirdparty.pparse.lib.extraction.BytesExtraction.open
```

Return ``Reader`` positioned at the start of the data.

**Returns:**

  - `Reader` - A duplicated ``Reader`` at offset 0.

[]{#thirdparty.pparse.lib.extraction.BytesExtraction.tell}

:::: {.member-rule}
::::

#### tell

```text
thirdparty.pparse.lib.extraction.BytesExtraction.tell
```

Return the current byte offset of the internal reader.

**Returns:**

  - `int` - The current read position.

[]{#thirdparty.pparse.lib.extraction.BytesExtraction.from_xml}

:::: {.member-rule}
::::

#### from_xml

```text
thirdparty.pparse.lib.extraction.BytesExtraction.from_xml
```

Load and resume a ``BytesExtraction`` from an XML element.

Reads the ``<datasource />`` child element to create the underlying
data object, wraps it in a ``Range``, and recursively processes any
child extractions and result references.

**Parameters:**

  - `xml_src` - Any - An XML element or string describing the extraction.
  - `pparse_xml` - Optional[Any] - The parent ``PparseXml`` resolver, required when
result references (``<result />``) are present.

**Returns:**

  - `BytesExtraction` - A populated ``BytesExtraction`` with its reader set up.

**Raises:**

  - `BytesExtraction` - A populated ``BytesExtraction`` with its reader set up.

[]{#thirdparty.pparse.lib.extraction.BytesExtraction.to_xml}

:::: {.member-rule}
::::

#### to_xml

```text
thirdparty.pparse.lib.extraction.BytesExtraction.to_xml
```

[]{#thirdparty.pparse.lib.parser.Parser}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.parser.Parser

```python
class Parser()
```

docstring will go here

::: {.lefttable}
| Name                                                                            |
| ------------------------------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.parser.Parser.__init__)                     |
| [`_id`](#thirdparty.pparse.lib.parser.Parser._id)                               |
| [`_source`](#thirdparty.pparse.lib.parser.Parser._source)                       |
| [`current`](#thirdparty.pparse.lib.parser.Parser.current)                       |
| [`_all_states`](#thirdparty.pparse.lib.parser.Parser._all_states)               |
| [`_base_state_cls`](#thirdparty.pparse.lib.parser.Parser._base_state_cls)       |
| [`_init_state_as_cls`](#thirdparty.pparse.lib.parser.Parser._init_state_as_cls) |
| [`source`](#thirdparty.pparse.lib.parser.Parser.source)                         |
| [`scan_data`](#thirdparty.pparse.lib.parser.Parser.scan_data)                   |
| [`match_extension`](#thirdparty.pparse.lib.parser.Parser.match_extension)       |
| [`match_magic`](#thirdparty.pparse.lib.parser.Parser.match_magic)               |
:::

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.parser.Parser.__init__
```

[]{#thirdparty.pparse.lib.parser.Parser._id}

:::: {.member-rule}
::::

#### _id

```text
thirdparty.pparse.lib.parser.Parser._id
```

[]{#thirdparty.pparse.lib.parser.Parser._source}

:::: {.member-rule}
::::

#### _source

```text
thirdparty.pparse.lib.parser.Parser._source
```

[]{#thirdparty.pparse.lib.parser.Parser.current}

:::: {.member-rule}
::::

#### current

```text
thirdparty.pparse.lib.parser.Parser.current
```

[]{#thirdparty.pparse.lib.parser.Parser._all_states}

:::: {.member-rule}
::::

#### _all_states

```text
thirdparty.pparse.lib.parser.Parser._all_states
```

[]{#thirdparty.pparse.lib.parser.Parser._base_state_cls}

:::: {.member-rule}
::::

#### _base_state_cls

```text
thirdparty.pparse.lib.parser.Parser._base_state_cls
```

[]{#thirdparty.pparse.lib.parser.Parser._init_state_as_cls}

:::: {.member-rule}
::::

#### _init_state_as_cls

```text
thirdparty.pparse.lib.parser.Parser._init_state_as_cls
```

Resolve ``init_state`` to a state class, validating it in the process.

Accepts either a string name (looked up in ``_all_states``) or an
actual class object (validated as a subclass of ``_base_state_cls``).
(Required for XML load and resume.)

**Parameters:**

  - `init_state` - Union[str, Type[Any]] - A state class or the string name of a registered state.

**Returns:**

  - `Type[Any]` - The resolved state class.

**Raises:**

  - `Type[Any]` - The resolved state class.

[]{#thirdparty.pparse.lib.parser.Parser.source}

:::: {.member-rule}
::::

#### source

```text
thirdparty.pparse.lib.parser.Parser.source
```

Return the ``Extraction`` this parser is consuming.

**Returns:**

  - `Extraction` - The parent ``Extraction`` instance.

[]{#thirdparty.pparse.lib.parser.Parser.scan_data}

:::: {.member-rule}
::::

#### scan_data

```text
thirdparty.pparse.lib.parser.Parser.scan_data
```

Parse the entire data source eagerly, populating the node tree.

Note: Legacy / Unused

**Raises:**


[]{#thirdparty.pparse.lib.parser.Parser.match_extension}

:::: {.member-rule}
::::

#### match_extension

```text
thirdparty.pparse.lib.parser.Parser.match_extension
```

Return whether the given filename suggests this parser can handle the data.

**Parameters:**

  - `fname` - str - The filename or path to test.

**Returns:**

  - `bool` - The base implementation always returns ``False``.

[]{#thirdparty.pparse.lib.parser.Parser.match_magic}

:::: {.member-rule}
::::

#### match_magic

```text
thirdparty.pparse.lib.parser.Parser.match_magic
```

Return whether the magic bytes at the start of the data match this format.

**Parameters:**

  - `cursor` - Any - A ``Reader`` positioned at the beginning of the data.

**Returns:**

  - `bool` - The base implementation always returns ``False``.

[]{#thirdparty.pparse.lib.parser.Parser.__init__}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.parser.Parser.__init__

::: {.lefttable}
| Name                                                                             |
| -------------------------------------------------------------------------------- |
| [`all_subclasses`](#thirdparty.pparse.lib.parser.Parser.__init__.all_subclasses) |
:::

[]{#thirdparty.pparse.lib.parser.Parser.__init__.all_subclasses}

:::: {.member-rule}
::::

#### all_subclasses

```text
thirdparty.pparse.lib.parser.Parser.__init__.all_subclasses
```

[]{#thirdparty.pparse.lib.tensor.Tensor}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.tensor.Tensor

```python
class Tensor()
```

docstring will go here

::: {.lefttable}
| Name                                                                    |
| ----------------------------------------------------------------------- |
| [`STTYPE_STRUCT`](#thirdparty.pparse.lib.tensor.Tensor.STTYPE_STRUCT)   |
| [`STTYPE_SIZE`](#thirdparty.pparse.lib.tensor.Tensor.STTYPE_SIZE)       |
| [`STTYPE_NP_MAP`](#thirdparty.pparse.lib.tensor.Tensor.STTYPE_NP_MAP)   |
| [`get_type`](#thirdparty.pparse.lib.tensor.Tensor.get_type)             |
| [`get_shape`](#thirdparty.pparse.lib.tensor.Tensor.get_shape)           |
| [`get_data_bytes`](#thirdparty.pparse.lib.tensor.Tensor.get_data_bytes) |
| [`as_array`](#thirdparty.pparse.lib.tensor.Tensor.as_array)             |
| [`as_numpy`](#thirdparty.pparse.lib.tensor.Tensor.as_numpy)             |
:::

[]{#thirdparty.pparse.lib.tensor.Tensor.STTYPE_STRUCT}

:::: {.member-rule}
::::

#### STTYPE_STRUCT

```text
thirdparty.pparse.lib.tensor.Tensor.STTYPE_STRUCT
```

[]{#thirdparty.pparse.lib.tensor.Tensor.STTYPE_SIZE}

:::: {.member-rule}
::::

#### STTYPE_SIZE

```text
thirdparty.pparse.lib.tensor.Tensor.STTYPE_SIZE
```

[]{#thirdparty.pparse.lib.tensor.Tensor.STTYPE_NP_MAP}

:::: {.member-rule}
::::

#### STTYPE_NP_MAP

```text
thirdparty.pparse.lib.tensor.Tensor.STTYPE_NP_MAP
```

[]{#thirdparty.pparse.lib.tensor.Tensor.get_type}

:::: {.member-rule}
::::

#### get_type

```text
thirdparty.pparse.lib.tensor.Tensor.get_type
```

Return the safetensors-compatible dtype string for this tensor.

**Returns:**

  - `str` - A dtype string such as ``"F32"``, ``"I64"``, etc.

[]{#thirdparty.pparse.lib.tensor.Tensor.get_shape}

:::: {.member-rule}
::::

#### get_shape

```text
thirdparty.pparse.lib.tensor.Tensor.get_shape
```

Return the safetensors-compatible shape of this tensor.

**Returns:**

  - `List[int]` - A list of dimension sizes, e.g. ``[batch, channels, height, width]``.

[]{#thirdparty.pparse.lib.tensor.Tensor.get_data_bytes}

:::: {.member-rule}
::::

#### get_data_bytes

```text
thirdparty.pparse.lib.tensor.Tensor.get_data_bytes
```

Return the raw tensor data exactly as it appears in the source.

**Returns:**

  - `bytes` - The raw byte content of the tensor.

[]{#thirdparty.pparse.lib.tensor.Tensor.as_array}

:::: {.member-rule}
::::

#### as_array

```text
thirdparty.pparse.lib.tensor.Tensor.as_array
```

Return the tensor data as a Python array typed to the tensor's dtype.

**Returns:**

  - `Any` - A Python sequence or array object representing the tensor values.

[]{#thirdparty.pparse.lib.tensor.Tensor.as_numpy}

:::: {.member-rule}
::::

#### as_numpy

```text
thirdparty.pparse.lib.tensor.Tensor.as_numpy
```

Return the tensor data as a NumPy array typed to the tensor's dtype and shape.

**Returns:**

  - `numpy.ndarray` - A ``numpy.ndarray`` with the appropriate dtype and shape.

[]{#thirdparty.pparse.lib.pparsexml.PparseXml}

:::: {.namespace-rule}
::::

### thirdparty.pparse.lib.pparsexml.PparseXml

```python
class PparseXml()
```

docstring will go here

::: {.lefttable}
| Name                                                                                                |
| --------------------------------------------------------------------------------------------------- |
| [`__init__`](#thirdparty.pparse.lib.pparsexml.PparseXml.__init__)                                   |
| [`xml`](#thirdparty.pparse.lib.pparsexml.PparseXml.xml)                                             |
| [`_result_ref_to_extraction`](#thirdparty.pparse.lib.pparsexml.PparseXml._result_ref_to_extraction) |
| [`add_result_ref`](#thirdparty.pparse.lib.pparsexml.PparseXml.add_result_ref)                       |
| [`has_extraction`](#thirdparty.pparse.lib.pparsexml.PparseXml.has_extraction)                       |
| [`get_extraction`](#thirdparty.pparse.lib.pparsexml.PparseXml.get_extraction)                       |
| [`from_xml`](#thirdparty.pparse.lib.pparsexml.PparseXml.from_xml)                                   |
:::

[]{#thirdparty.pparse.lib.pparsexml.PparseXml.__init__}

:::: {.member-rule}
::::

#### __init__

```text
thirdparty.pparse.lib.pparsexml.PparseXml.__init__
```

[]{#thirdparty.pparse.lib.pparsexml.PparseXml.xml}

:::: {.member-rule}
::::

#### xml

```text
thirdparty.pparse.lib.pparsexml.PparseXml.xml
```

[]{#thirdparty.pparse.lib.pparsexml.PparseXml._result_ref_to_extraction}

:::: {.member-rule}
::::

#### _result_ref_to_extraction

```text
thirdparty.pparse.lib.pparsexml.PparseXml._result_ref_to_extraction
```

[]{#thirdparty.pparse.lib.pparsexml.PparseXml.add_result_ref}

:::: {.member-rule}
::::

#### add_result_ref

```text
thirdparty.pparse.lib.pparsexml.PparseXml.add_result_ref
```

Register map between result ID and ``Extraction``.

**Parameters:**

  - `result_ref_id` - int - The integer ``id`` attribute from a ``<result />``
XML element.
  - `extraction` - BytesExtraction - The ``BytesExtraction`` that owns the result.

[]{#thirdparty.pparse.lib.pparsexml.PparseXml.has_extraction}

:::: {.member-rule}
::::

#### has_extraction

```text
thirdparty.pparse.lib.pparsexml.PparseXml.has_extraction
```

Return whether a result ID has a registered extraction.

**Parameters:**

  - `result_ref_id` - int - The result ID to look up.

**Returns:**

  - `bool` - ``True`` if the ID is registered, ``False`` otherwise.

[]{#thirdparty.pparse.lib.pparsexml.PparseXml.get_extraction}

:::: {.member-rule}
::::

#### get_extraction

```text
thirdparty.pparse.lib.pparsexml.PparseXml.get_extraction
```

Return the extraction registered for a result ID.

**Parameters:**

  - `result_ref_id` - int - The result ID to look up.

**Returns:**

  - `BytesExtraction` - The ``BytesExtraction`` associated with ``result_ref_id``.

[]{#thirdparty.pparse.lib.pparsexml.PparseXml.from_xml}

:::: {.member-rule}
::::

#### from_xml

```text
thirdparty.pparse.lib.pparsexml.PparseXml.from_xml
```

Load and resume from a ``<pparse />`` XML document into a live object graph.

Recursively imports the root extraction, all result references, and
all ``<node />`` elements, restoring the parser and context state for
each node.

**Parameters:**

  - `xml_src` - Any - A string, file path, or XmlNode representing the
``<pparse />`` document root.

**Returns:**

  - `PparseXml` - node references resolved.

**Raises:**

  - `PparseXml` - node references resolved.


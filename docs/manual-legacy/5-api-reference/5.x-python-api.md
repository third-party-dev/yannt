<Remove>{/*<!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT --><!-- AUTO GENERATED FILE, DO NOT EDIT -->*/}</Remove>
<a id="thirdparty.pparse.lib.node"></a>

# node

Node — the core data unit of the pparse parse-tree.

<a id="thirdparty.pparse.lib.node.UnloadedValue"></a>

## UnloadedValue

```python
class UnloadedValue()
```

Sentinel object used as the default value of a ``Node`` before it is parsed.

A single module-level instance ``UNLOADED_VALUE`` is used everywhere.

<a id="thirdparty.pparse.lib.node.RecursionControl"></a>

## RecursionControl

```python
class RecursionControl()
```

Configurable guard that limits how deeply ``Node.load`` recurses.

``RecursionControl`` is passed to ``Node.load`` to give callers
fine-grained control over which parts of the parse tree are eagerly
expanded.  At each node, ``stopped`` is called to decide whether
recursion should halt.

**Arguments**:

- `min_depth` - Nodes at depths below this value are always loaded,
  ignoring ``max_depth`` and the callback.
- `max_depth` - Nodes at depths above this value are never loaded.
- `callback` - An optional callable that receives a ``Node`` and returns
  ``True`` to stop recursion at that node.  Only called when
  ``min_depth <= cur_depth <= max_depth``.

<a id="thirdparty.pparse.lib.node.RecursionControl.stopped"></a>

#### RecursionControl.stopped

```python
def stopped(node: Node) -> bool
```

Return whether recursion should halt at ``node``.

**Arguments**:

- `node` - The ``Node`` that is about to be loaded.
  

**Returns**:

  ``True`` if loading ``node`` should be skipped, ``False`` if it
  should proceed.  Returns ``None`` (falsy) when depth is within
  the min/max window and no callback is configured.

<a id="thirdparty.pparse.lib.node.RecursionControl.increase_depth"></a>

#### RecursionControl.increase\_depth

```python
def increase_depth(amount: int = 1) -> None
```

Increment the current recursion depth and update the high-water mark.

**Arguments**:

- `amount` - Number of levels to increase by.

<a id="thirdparty.pparse.lib.node.RecursionControl.decrease_depth"></a>

#### RecursionControl.decrease\_depth

```python
def decrease_depth(amount: int = 1) -> None
```

Decrement the current recursion depth.

**Arguments**:

- `amount` - Number of levels to decrease by.

<a id="thirdparty.pparse.lib.node.RecursionControl.current_depth"></a>

#### RecursionControl.current\_depth

```python
def current_depth() -> int
```

Return the current recursion depth.

**Returns**:

  The number of ``load`` calls currently on the call stack.

<a id="thirdparty.pparse.lib.node.RecursionControl.deepest_depth"></a>

#### RecursionControl.deepest\_depth

```python
def deepest_depth() -> int
```

Return the maximum recursion depth seen since this instance was created.

**Returns**:

  The high-water mark for recursion depth.

<a id="thirdparty.pparse.lib.node.Node"></a>

## Node

```python
class Node()
```

A single node in the pparse parse-tree.

A ``Node`` represents a region of binary data that a ``Parser`` knows about
and _possibly_ how to interpret.  Its ``_value`` starts as ``UNLOADED_VALUE`` and is
populated lazily on first access via the ``value`` property, which
triggers ``load()``.

Each node owns a ``NodeContext`` that carries the parser's state machine (for the Node)
and a ``Reader`` positioned at the start of the node's data offset.

**Arguments**:

- `reader` - A ``Reader`` positioned at the start of this node's data.
- `parser` - The ``Parser`` responsible for interpreting this node.
- `default_value` - Initial value; defaults to ``UNLOADED_VALUE`` so that
  the node is parsed lazily on first access.
- `parent` - The parent ``Node``, or ``None`` for the root.
- `ctx_class` - An optional ``NodeContext`` subclass to instantiate instead
  of the default ``NodeContext``.
- `ctx_args` - Extra keyword arguments forwarded to ``ctx_class``.

<a id="thirdparty.pparse.lib.node.Node.value"></a>

#### Node.value

```python
@property
def value() -> Any
```

Return the parsed value, triggering ``load()`` if not yet parsed.

**Returns**:

  The node's parsed value (dict, list, scalar, or nested ``Node``).

<a id="thirdparty.pparse.lib.node.Node.ctx"></a>

#### Node.ctx

```python
def ctx() -> NodeContext
```

Return the ``NodeContext`` for this node.

**Returns**:

  The ``NodeContext`` holding the parser state for this node.

<a id="thirdparty.pparse.lib.node.Node.clear_ctx"></a>

#### Node.clear\_ctx

```python
def clear_ctx() -> Node
```

Release the ``NodeContext`` to free memory.

Should only be called once the node's value is fully loaded and
re-parsing is no longer needed or acceptably thrown away for restoration
of memory.

**Returns**:

  ``self``, to allow chaining.

<a id="thirdparty.pparse.lib.node.Node.tell"></a>

#### Node.tell

```python
def tell() -> int
```

Return the absolute byte offset of the start of this node's data.

**Returns**:

  The node's start position within the data source.

<a id="thirdparty.pparse.lib.node.Node.set_length"></a>

#### Node.set\_length

```python
def set_length(length: int) -> Node
```

Replace the internal reader with a ``Range`` of ``length`` bytes.

**Arguments**:

- `length` - Number of bytes in this node's data region.
  

**Returns**:

  ``self``, to allow chaining.

<a id="thirdparty.pparse.lib.node.Node.length"></a>

#### Node.length

```python
def length() -> int
```

Return the byte length of this node's data region.

**Returns**:

  The number of bytes in the node's ``Range``.

<a id="thirdparty.pparse.lib.node.Node.load"></a>

#### Node.load

```python
def load(recursion: Optional[RecursionControl] = None) -> Optional[Node]
```

Parse this node's data region and populate ``_value``.

Drives the parser state machine until ``EndOfNodeException`` (success),
``EndOfDataException`` (propagated), or ``UnsupportedFormatException``
(propagated) is raised.  Whenever the state machine registers
descendant nodes they are immediately loaded in depth-first order,
subject to ``recursion`` constraints.

``load()`` manages ``RecursionControl`` lifetime, enabling different
behaviors per call and managing recursion depth relative to the
current node.

CAUTION: Recursion control is a mechanism that allows parsers to delegate recursion
decisions to the caller or user of the API. It does not enforce or provide
tight controls over the parts of the Node tree that get processed.

RULE: load() should handle the recursive behavior, not the parser state.

RULE: Leaky abstraction when recursion policy stored in Node or NodeContext!

NOTE: json parsing is weird because you have to recurse every time. Since I
implemented JSON first, I believe its driving some of the anti-patterns in pparse.
If we want to save on memory for JSON, we could theoretically parse and allocate
nodes and as we complete branches of a depth first parse we deallocated.
I naturally want to do this for recursive=False, but since recursive=False
is default, it makes it feel like unnecessary thrashing of the CPU.

**Arguments**:

- `recursion` - An optional ``RecursionControl`` that limits how deeply
  the parse tree is expanded.  When ``None`` all reachable
  descendants are loaded eagerly.
  

**Returns**:

  ``self`` if loading completed, ``None`` if ``recursion`` halted
  loading before any state was run.
  

**Raises**:

- `EndOfDataException` - If the data source is exhausted unexpectedly.
- `UnsupportedFormatException` - If the data does not match the expected
  format.

<a id="thirdparty.pparse.lib.node.Node.dump"></a>

#### Node.dump

```python
def dump(depth: int = 0, step: int = 2, dumper: Any = None) -> None
```

Render this node to the given dumper for debugging or serialization.

**Arguments**:

- `depth` - Current indentation depth passed to the dumper.
- `step` - Number of spaces per indentation level.
- `dumper` - A ``Dumper`` instance; ``Dumper.default()`` is used when
  this is ``None``.

<a id="thirdparty.pparse.lib.node.Node.from_xml"></a>

#### Node.from\_xml

```python
@classmethod
def from_xml(cls, src_xml: Any, ctx_ref: Any) -> Optional[Node]
```

Reconstruct a ``Node`` from a ``<node />`` XML element.

Reads parser, context, and value information from the XML, resolves
the appropriate parser via a built-in registry, and recursively
deserializes any nested ``<node />`` values.

**Arguments**:

- `src_xml` - A ``<node />`` XML element or string.
- `ctx_ref` - A ``ContextRef`` carrying the parent result reference,
  optional context XML, and resolved parser.
  

**Returns**:

  The reconstructed ``Node``, or ``None`` if the node has no
  ``<value />`` element (i.e. is left in the unloaded state).
  

**Raises**:

- `Exception` - If the element is not a ``<node />`` or required
  attributes are missing.

<a id="thirdparty.pparse.lib.exceptions"></a>

# exceptions

Exceptions raised by the pparse parsing framework.

<a id="thirdparty.pparse.lib.exceptions.EndOfDataException"></a>

## EndOfDataException

```python
class EndOfDataException(Exception)
```

Raised when the end of the data currently available is reached.

<a id="thirdparty.pparse.lib.exceptions.EndOfNodeException"></a>

## EndOfNodeException

```python
class EndOfNodeException(Exception)
```

Raised when the parser has finished processing the current node.

<a id="thirdparty.pparse.lib.exceptions.UnsupportedFormatException"></a>

## UnsupportedFormatException

```python
class UnsupportedFormatException(Exception)
```

Raised when the observed data is not recognized by the parser.

<a id="thirdparty.pparse.lib.exceptions.BufferFullException"></a>

## BufferFullException

```python
class BufferFullException(Exception)
```

Raised when a buffer cannot accept additional data.

<a id="thirdparty.pparse.lib.extraction"></a>

# extraction

Extraction classes that couple a data source to one or more parsers.

<a id="thirdparty.pparse.lib.extraction.Extraction"></a>

## Extraction

```python
class Extraction()
```

Abstract representation of a named data source with zero or more attached parsers.

An ``Extraction`` couples a raw data source (file, URL, in-memory buffer)
with the parsers that know how to interpret it.  Subclasses implement
``open()`` to return a ``Reader`` positioned at the start of the data.

**Arguments**:

- `name` - A human-readable name for this extraction (e.g. a filename).
- `source` - The parent this ``Extraction`` was derived from, if any.

<a id="thirdparty.pparse.lib.extraction.Extraction.name"></a>

#### Extraction.name

```python
def name() -> Optional[str]
```

Return the name of this extraction.

**Returns**:

  The extraction name, or ``None`` if not set.

<a id="thirdparty.pparse.lib.extraction.Extraction.set_name"></a>

#### Extraction.set\_name

```python
def set_name(name: str) -> Extraction
```

Set the name of this extraction.

**Arguments**:

- `name` - The new name.
  

**Returns**:

  ``self``, to allow chaining.

<a id="thirdparty.pparse.lib.extraction.Extraction.add_result"></a>

#### Extraction.add\_result

```python
def add_result(id: Any, root_node: Optional[Node]) -> None
```

Register a root node against a result slot.

**Arguments**:

- `id` - The result identifier (typically a string key).
- `root_node` - The root ``Node`` of the parse result, or ``None`` if
  the result has not been populated yet.

<a id="thirdparty.pparse.lib.extraction.Extraction.add_parser"></a>

#### Extraction.add\_parser

```python
def add_parser(id: str, parser: Optional[Parser]) -> None
```

Register a parser under a string identifier.

**Arguments**:

- `id` - A unique string key for this parser.
- `parser` - The ``Parser`` instance to register.

<a id="thirdparty.pparse.lib.extraction.Extraction.has_parser"></a>

#### Extraction.has\_parser

```python
def has_parser(parser_id: str) -> bool
```

Return whether a parser with the given ID has been registered.

**Arguments**:

- `parser_id` - The parser identifier to check.
  

**Returns**:

  ``True`` if a parser with that ID exists, ``False`` otherwise.

<a id="thirdparty.pparse.lib.extraction.Extraction.discover_parsers"></a>

#### Extraction.discover\_parsers

```python
def discover_parsers(parser_registry: Dict[str, Any]) -> Extraction
```

Auto-detect applicable parsers from a registry and register them.

For each parser in ``parser_registry`` that has not already been
registered, the method tries ``match_extension`` on the extraction
name and then ``match_magic`` on the data bytes.  The first matching
parser is instantiated and registered.

**Arguments**:

- `parser_registry` - A mapping of parser ID strings to parser classes.
  

**Returns**:

  ``self``, to allow chaining.

<a id="thirdparty.pparse.lib.extraction.Extraction.open"></a>

#### Extraction.open

```python
def open() -> Reader
```

Return a fresh ``Reader`` positioned at the start of the ``Extraction``'s data.

**Returns**:

  A ``Reader`` ready for use at offset 0.
  

**Raises**:

- `NotImplementedError` - Must be implemented by subclasses.

<a id="thirdparty.pparse.lib.extraction.Extraction.scan_data"></a>

#### Extraction.scan\_data

```python
def scan_data() -> Extraction
```

Trigger all registered parsers to scan the full data source.

Note: Legacy/Unused

**Returns**:

  ``self``, to allow chaining.

<a id="thirdparty.pparse.lib.extraction.Extraction.from_xml"></a>

#### Extraction.from\_xml

```python
@classmethod
def from_xml(cls, xml_src: Any, xml_root: Any) -> Extraction
```

Load and resume ``Extraction`` from state in XML.

**Arguments**:

- `xml_src` - An XML element or string describing the extraction.
- `xml_root` - The root XML context used to resolve references.
  

**Returns**:

  A populated ``Extraction`` instance.
  

**Raises**:

- `NotImplementedError` - Must be implemented by subclasses.

<a id="thirdparty.pparse.lib.extraction.Extraction.to_xml"></a>

#### Extraction.to\_xml

```python
def to_xml() -> str
```

Stop and save the current state of ``Extraction`` to XML.

**Returns**:

  An XML string representation of this extraction.
  

**Raises**:

- `NotImplementedError` - Must be implemented by subclasses.

<a id="thirdparty.pparse.lib.extraction.BytesExtraction"></a>

## BytesExtraction

```python
class BytesExtraction(Extraction)
```

An ``Extraction`` backed by an existing ``Reader`` (bytes already in hand).

Used when the raw data is accessible via a ``Reader`` — for example, a
``Range`` wrapping a memory-mapped file or a ``BytesIoData`` cursor.
Exactly one of ``source`` or ``reader`` must be provided.

**Arguments**:

- `name` - A human-readable name for this extraction (e.g. relative filename).
- `source` - Parent ``Extraction`` to ``open`` a ``Reader``.
- `reader` - An existing ``Reader`` to use directly.
  
- `Note` - Use only source xor reader, not both (i.e. one and only one must be set).
  

**Raises**:

- `ValueError` - If both or neither of ``source`` and ``reader`` are provided.

<a id="thirdparty.pparse.lib.extraction.BytesExtraction.open"></a>

#### BytesExtraction.open

```python
def open() -> Reader
```

Return ``Reader`` positioned at the start of the data.

**Returns**:

  A duplicated ``Reader`` at offset 0.

<a id="thirdparty.pparse.lib.extraction.BytesExtraction.tell"></a>

#### BytesExtraction.tell

```python
def tell() -> int
```

Return the current byte offset of the internal reader.

**Returns**:

  The current read position.

<a id="thirdparty.pparse.lib.extraction.BytesExtraction.from_xml"></a>

#### BytesExtraction.from\_xml

```python
@classmethod
def from_xml(cls,
             xml_src: Any,
             pparse_xml: Optional[Any] = None) -> BytesExtraction
```

Load and resume a ``BytesExtraction`` from an XML element.

Reads the ``<datasource />`` child element to create the underlying
data object, wraps it in a ``Range``, and recursively processes any
child extractions and result references.

**Arguments**:

- `xml_src` - An XML element or string describing the extraction.
- `pparse_xml` - The parent ``PparseXml`` resolver, required when
  result references (``<result />``) are present.
  

**Returns**:

  A populated ``BytesExtraction`` with its reader set up.
  

**Raises**:

- `Exception` - If required attributes or elements are missing, or if
  result references are present but no resolver is provided.

<a id="thirdparty.pparse.lib.data"></a>

# data

Data source implementations that back ``Cursor`` and ``Range`` objects.

<a id="thirdparty.pparse.lib.data.Data"></a>

## Data

```python
class Data()
```

Abstract data backend that provides I/O operations to ``Cursor`` objects.

``Data`` subclasses own the access to persistent storage (file, HTTP URL, mmap,
in-memory ``BytesIO``).  ``Data`` implements ``peek`` for non-advancing reads;
seek and read are provided with default implementations built on top of
``peek``.

The design keeps offset tracking out of ``Data``: a ``Cursor`` manages
the current position and passes itself to ``Data`` methods as a handle. A
``Cursor`` is not unlike a user-space defined file descriptor.

<a id="thirdparty.pparse.lib.data.Data.open"></a>

#### Data.open

```python
def open(offset: int = 0) -> Cursor
```

Return a new ``Cursor`` into this data source at ``offset``.

**Arguments**:

- `offset` - Initial byte offset for the cursor.
  

**Returns**:

  A ``Cursor`` positioned at ``offset``.

<a id="thirdparty.pparse.lib.data.Data.peek"></a>

#### Data.peek

```python
def peek(cursor: Cursor, length: int) -> bytes
```

Read ``length`` bytes at the cursor's position without advancing offset.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.
  

**Raises**:

- `NotImplementedError` - Must be implemented by subclasses.

<a id="thirdparty.pparse.lib.data.Data.seek"></a>

#### Data.seek

```python
def seek(cursor: Cursor) -> int
```

Update the underlying file descriptor or upstream libraries internal offset.

The base implementation is a no-op that returns the cursor's current
position.  Subclasses that maintain their own file pointer should
override ``seek``.

**Arguments**:

- `cursor` - The ``Cursor`` whose offset should be applied.
  

**Returns**:

  The cursor's current byte offset.

<a id="thirdparty.pparse.lib.data.Data.read"></a>

#### Data.read

```python
def read(cursor: Cursor, length: int) -> bytes
```

Read ``length`` bytes.

The base implementation uses ``peek``.  Subclasses that can perform a more
efficient read-and-seek in a single syscall should override ``read``.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Max number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.data.HttpCachedData"></a>

## HttpCachedData

```python
class HttpCachedData(Data)
```

``Data`` backend that fetches remote content via HTTP with a local chunk cache.

Downloads the target resource in fixed-size chunks and keeps a bounded
LRU-style cache in memory.  Supports servers that advertise
``Accept-Ranges: bytes``; falls back to a "download from beginning" when Range
requests are not available.

**Arguments**:

- `url` - The full HTTP/HTTPS URL of the target resource.
- `chunk_size` - Size of each cache chunk in bytes. (Ideally close to page size or hardware cache size)
- `chunk_max_count` - Maximum number of chunks to keep in the cache (consider SDRAM available to process).
- `session` - An optional ``requests.Session`` to use for HTTP calls.
  A new session is created when this is ``None``.
  

**Raises**:

- `Exception` - If Range requests are not supported and the file is larger
  than the total cache capacity.

<a id="thirdparty.pparse.lib.data.HttpCachedData.peek"></a>

#### HttpCachedData.peek

```python
def peek(cursor: Cursor, length: int) -> bytes
```

Read ``length`` bytes at the cursor's position via the chunk cache.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.data.FileData"></a>

## FileData

```python
class FileData(Data)
```

``Data`` backend that reads from a local file via a buffer.

Opens the file in binary read mode and uses ``seek()`` + ``read()`` syscalls
for each access.  The file object is kept open for the lifetime of the
instance (shared with all associated ``Reader``s).

**Arguments**:

- `path` - Absolute or relative path to the file to read.
  

**Raises**:

- `ValueError` - If ``path`` is empty, ``None``, or does not exist.

<a id="thirdparty.pparse.lib.data.FileData.peek"></a>

#### FileData.peek

```python
def peek(cursor: Cursor, length: int) -> bytes
```

Seek the file to the cursor's position and read ``length`` bytes.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.data.FileData.seek"></a>

#### FileData.seek

```python
def seek(cursor: Cursor) -> int
```

Update file descriptor to the cursor's byte offset.

**Arguments**:

- `cursor` - The ``Cursor`` whose offset to apply.
  

**Returns**:

  The cursor's current byte offset.

<a id="thirdparty.pparse.lib.data.FileData.read"></a>

#### FileData.read

```python
def read(cursor: Cursor, length: int) -> bytes
```

Read ``length`` bytes.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Max number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.data.FileData.from_xml"></a>

#### FileData.from\_xml

```python
@classmethod
def from_xml(cls, xml_src: Any) -> FileData
```

Deserialize a ``FileData`` from a ``<datasource />`` XML element.

Reads the ``posix_path`` (or ``windows_path``) from the element's
``<extra />`` child and attempts to validate the file is unchanges by
comparing the on-disk file length to the XML recorded length.

**Arguments**:

- `xml_src` - An XML element or string for the ``<datasource />`` node.
  

**Returns**:

  A ``FileData`` instance opened on the described file.
  

**Raises**:

- `Exception` - If the element tag is wrong, a path key is missing, or
  the recorded length does not match the file on disk.

<a id="thirdparty.pparse.lib.data.FileMmapData"></a>

## FileMmapData

```python
class FileMmapData(Data)
```

``Data`` backend that memory-maps a local file for zero-copy access.

Uses ``mmap`` and a ``memoryview`` overlay so that slice operations
return a ``memoryview`` without copying bytes.  This is the fastest
backend for local files when random access patterns are common.

Note: Untested in production use. (Unused/Legacy)

**Arguments**:

- `path` - Absolute or relative path to the file to map.
  

**Raises**:

- `ValueError` - If ``path`` is empty, ``None``, or does not exist.
- `Exception` - If ``mmap`` is not available on the platform.

<a id="thirdparty.pparse.lib.data.FileMmapData.peek"></a>

#### FileMmapData.peek

```python
def peek(cursor: Cursor, length: int) -> memoryview
```

Return a zero-copy ``memoryview`` slice at the cursor's position.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Number of bytes to expose.
  

**Returns**:

  A ``memoryview`` over the requested byte range.

<a id="thirdparty.pparse.lib.data.FileMmapData.seek"></a>

#### FileMmapData.seek

```python
def seek(cursor: Cursor) -> int
```

No-op seek — mmap access is always random, so no pointer sync is needed.

**Arguments**:

- `cursor` - The ``Cursor`` whose position to return.
  

**Returns**:

  The cursor's current byte offset.

<a id="thirdparty.pparse.lib.data.FileMmapData.read"></a>

#### FileMmapData.read

```python
def read(cursor: Cursor, length: int, mode: Any = None) -> memoryview
```

Return a zero-copy ``memoryview`` slice at the cursor's position.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Number of bytes to expose.
- `mode` - Unused; present for interface compatibility.
  

**Returns**:

  A ``memoryview`` over the requested byte range.

<a id="thirdparty.pparse.lib.data.BytesIoData"></a>

## BytesIoData

```python
class BytesIoData(Data)
```

``Data`` backend backed by an in-memory ``io.BytesIO`` buffer.

Useful when the raw bytes are already in memory, e.g. when a file inside
a ZIP archive has been decompressed into a buffer and needs to be parsed
as its own extraction.

**Arguments**:

- `bytes_io` - The ``BytesIO`` buffer to read from.
  

**Raises**:

- `ValueError` - If ``bytes_io`` is ``None`` or is not a ``BytesIO`` instance.

<a id="thirdparty.pparse.lib.data.BytesIoData.open"></a>

#### BytesIoData.open

```python
def open(offset: int = 0) -> Cursor
```

Return a new ``Cursor`` into this buffer at ``offset``.

**Arguments**:

- `offset` - Initial byte offset for the cursor.
  

**Returns**:

  A ``Cursor`` positioned at ``offset``.

<a id="thirdparty.pparse.lib.data.BytesIoData.peek"></a>

#### BytesIoData.peek

```python
def peek(cursor: Cursor, length: int) -> bytes
```

Seek the buffer to the cursor's position and read ``length`` bytes.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.data.BytesIoData.seek"></a>

#### BytesIoData.seek

```python
def seek(cursor: Cursor) -> int
```

Update ``BytesIO`` internal position to the cursor's byte offset.

**Arguments**:

- `cursor` - The ``Cursor`` whose offset to apply.
  

**Returns**:

  The cursor's current byte offset.

<a id="thirdparty.pparse.lib.data.BytesIoData.read"></a>

#### BytesIoData.read

```python
def read(cursor: Cursor, length: int) -> bytes
```

Read ``length`` bytes.

**Arguments**:

- `cursor` - The ``Cursor`` indicating where to read from.
- `length` - Max number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.node_context"></a>

# node\_context

NodeContext — per-node parser state machine and parser bookkeeping.

<a id="thirdparty.pparse.lib.node_context.NodeContext"></a>

## NodeContext

```python
class NodeContext()
```

Holds the parser state and reader position associated with a single ``Node``.

``NodeContext`` is the glue between a ``Node`` and its ``Parser``.  It
owns the parser's state stack for the node and provides pass-through
reader methods so that parser state objects do not need a direct
reference to the underlying ``Reader``. It's also an all in one reference
to cut once parsing is complete and we need to restore memory.

**Arguments**:

- `parent` - The parent ``Node``, or ``None`` for the root.
- `reader` - The ``Reader`` positioned at the start of this node's data.
- `parser` - The ``Parser`` responsible for interpreting this node.

<a id="thirdparty.pparse.lib.node_context.NodeContext.parent"></a>

#### NodeContext.parent

```python
def parent() -> Optional[Node]
```

Return the parent node of this context's node.

**Returns**:

  The parent ``Node``, or ``None`` if this is the root node.

<a id="thirdparty.pparse.lib.node_context.NodeContext.parent_ctx"></a>

#### NodeContext.parent\_ctx

```python
def parent_ctx() -> Optional[NodeContext]
```

Return the ``NodeContext`` of the parent node.

**Returns**:

  The parent's ``NodeContext``, or ``None`` if there is no parent.

<a id="thirdparty.pparse.lib.node_context.NodeContext.reader"></a>

#### NodeContext.reader

```python
def reader() -> Reader
```

Return an independent copy of the internal reader.

**Returns**:

  A duplicated ``Reader`` at the current read position.

<a id="thirdparty.pparse.lib.node_context.NodeContext.parser"></a>

#### NodeContext.parser

```python
def parser() -> Parser
```

Return the parser associated with this context.

**Returns**:

  The ``Parser`` instance for this node.

<a id="thirdparty.pparse.lib.node_context.NodeContext.state"></a>

#### NodeContext.state

```python
def state() -> Any
```

Return the current (top-of-stack) state instance.

**Returns**:

  The active parser state object.

<a id="thirdparty.pparse.lib.node_context.NodeContext.set_remaining"></a>

#### NodeContext.set\_remaining

```python
def set_remaining(length: int) -> None
```

Record the end of this node as ``current_position + length``.

**Arguments**:

- `length` - Number of bytes remaining in the node from the current position.

<a id="thirdparty.pparse.lib.node_context.NodeContext.mark_end"></a>

#### NodeContext.mark\_end

```python
def mark_end(node: Node) -> None
```

Record the current position as the end of the node and update its length.

**Arguments**:

- `node` - The ``Node`` whose length should be set based on the bytes consumed.

<a id="thirdparty.pparse.lib.node_context.NodeContext.mark_field_start"></a>

#### NodeContext.mark\_field\_start

```python
def mark_field_start() -> None
```

Record the current read position as the start of the next field.

<a id="thirdparty.pparse.lib.node_context.NodeContext.field_start"></a>

#### NodeContext.field\_start

```python
def field_start() -> int
```

Return the byte offset recorded by the most recent ``mark_field_start`` call.

**Returns**:

  The saved field-start byte offset.

<a id="thirdparty.pparse.lib.node_context.NodeContext.dup"></a>

#### NodeContext.dup

```python
def dup() -> Reader
```

Return an independent copy of the internal reader.

**Returns**:

  A duplicated ``Reader`` at the current read position.

<a id="thirdparty.pparse.lib.node_context.NodeContext.tell"></a>

#### NodeContext.tell

```python
def tell() -> int
```

Return the current byte offset of the internal reader.

**Returns**:

  The current read position.

<a id="thirdparty.pparse.lib.node_context.NodeContext.seek"></a>

#### NodeContext.seek

```python
def seek(*args: Any, **kwargs: Any) -> Any
```

Forward a seek call to the internal reader.

**Arguments**:

- `*args` - Positional arguments forwarded to ``Reader.seek``.
- `**kwargs` - Keyword arguments forwarded to ``Reader.seek``.
  

**Returns**:

  Whatever the underlying ``Reader.seek`` returns.

<a id="thirdparty.pparse.lib.node_context.NodeContext.skip"></a>

#### NodeContext.skip

```python
def skip(*args: Any, **kwargs: Any) -> Any
```

Forward a skip call to the internal reader.

**Arguments**:

- `*args` - Positional arguments forwarded to ``Reader.skip``.
- `**kwargs` - Keyword arguments forwarded to ``Reader.skip``.
  

**Returns**:

  Whatever the underlying ``Reader.skip`` returns.

<a id="thirdparty.pparse.lib.node_context.NodeContext.peek"></a>

#### NodeContext.peek

```python
def peek(*args: Any, **kwargs: Any) -> bytes
```

Forward a peek call to the internal reader.

**Arguments**:

- `*args` - Positional arguments forwarded to ``Reader.peek``.
- `**kwargs` - Keyword arguments forwarded to ``Reader.peek``.
  

**Returns**:

  Bytes read by the underlying ``Reader.peek``.

<a id="thirdparty.pparse.lib.node_context.NodeContext.read"></a>

#### NodeContext.read

```python
def read(*args: Any, **kwargs: Any) -> bytes
```

Forward a read call to the internal reader.

**Arguments**:

- `*args` - Positional arguments forwarded to ``Reader.read``.
- `**kwargs` - Keyword arguments forwarded to ``Reader.read``.
  

**Returns**:

  Bytes read by the underlying ``Reader.read``.

<a id="thirdparty.pparse.lib.node_context.NodeContext.left"></a>

#### NodeContext.left

```python
def left() -> int
```

Return bytes remaining between the current position and the range end.

The internal reader must be a ``Range`` for this to work.

**Returns**:

  Number of bytes remaining in the range.
  

**Raises**:

- `Exception` - If the internal reader is not a ``Range``.

<a id="thirdparty.pparse.lib.constants"></a>

# constants

pparse constants

<a id="thirdparty.pparse.lib.parser"></a>

# parser

Base ``Parser`` class for format-specific extraction parsers.

<a id="thirdparty.pparse.lib.parser.Parser"></a>

## Parser

```python
class Parser()
```

Abstract base class for format-specific parsers attached to an ``Extraction``.

A ``Parser`` is responsible for lazily interpreting the bytes provided by
its ``Extraction`` and populating a tree of ``Node`` objects.  Concrete
subclasses override ``match_extension`` and ``match_magic`` so the framework
can auto-detect the right parser for a given data source.

If ``base_state_cls`` is provided the constructor automatically discovers
all transitive subclasses and registers them by name in ``_all_states``,
enabling state lookup by string name via ``_init_state_as_cls``. (Required for
XML load and resume.)

**Arguments**:

- `source` - The ``Extraction`` this parser will consume.
- `id` - A unique string identifier for this parser within the extraction.
- `base_state_cls` - Optional base class for the parser's state machine.
  All subclasses of this class are registered in ``_all_states``.
  

**Raises**:

- `TypeError` - If ``source`` is not an ``Extraction`` instance.

<a id="thirdparty.pparse.lib.parser.Parser.source"></a>

#### Parser.source

```python
def source() -> Extraction
```

Return the ``Extraction`` this parser is consuming.

**Returns**:

  The parent ``Extraction`` instance.

<a id="thirdparty.pparse.lib.parser.Parser.scan_data"></a>

#### Parser.scan\_data

```python
def scan_data() -> None
```

Parse the entire data source eagerly, populating the node tree.

Note: Legacy / Unused

**Raises**:

- `NotImplementedError` - Must be implemented by each concrete subclass.

<a id="thirdparty.pparse.lib.parser.Parser.match_extension"></a>

#### Parser.match\_extension

```python
@staticmethod
def match_extension(fname: str) -> bool
```

Return whether the given filename suggests this parser can handle the data.

**Arguments**:

- `fname` - The filename or path to test.
  

**Returns**:

  ``True`` if the extension matches, ``False`` otherwise.
  The base implementation always returns ``False``.

<a id="thirdparty.pparse.lib.parser.Parser.match_magic"></a>

#### Parser.match\_magic

```python
@staticmethod
def match_magic(cursor: Any) -> bool
```

Return whether the magic bytes at the start of the data match this format.

**Arguments**:

- `cursor` - A ``Reader`` positioned at the beginning of the data.
  

**Returns**:

  ``True`` if the magic bytes match, ``False`` otherwise.
  The base implementation always returns ``False``.

<a id="thirdparty.pparse.lib.pparsexml"></a>

# pparsexml

XML-driven import/export helpers that bridge pparse's object model and XML.

<a id="thirdparty.pparse.lib.pparsexml.PparseXml"></a>

## PparseXml

```python
class PparseXml()
```

Coordinator for XML-driven import of a pparse parse session.

``PparseXml`` is instantiated once per ``<pparse />`` XML document.  It
maintains a mapping from numeric result IDs to the ``BytesExtraction``
objects that own those results, acting as a cross-reference resolver
during the recursive import of ``<node />`` elements.

**Arguments**:

- `xml` - The root XML node (or string) representing the ``<pparse />``
  document being imported.

<a id="thirdparty.pparse.lib.pparsexml.PparseXml.add_result_ref"></a>

#### PparseXml.add\_result\_ref

```python
def add_result_ref(result_ref_id: int, extraction: BytesExtraction) -> None
```

Register map between result ID and ``Extraction``.

**Arguments**:

- `result_ref_id` - The integer ``id`` attribute from a ``<result />``
  XML element.
- `extraction` - The ``BytesExtraction`` that owns the result.

<a id="thirdparty.pparse.lib.pparsexml.PparseXml.has_extraction"></a>

#### PparseXml.has\_extraction

```python
def has_extraction(result_ref_id: int) -> bool
```

Return whether a result ID has a registered extraction.

**Arguments**:

- `result_ref_id` - The result ID to look up.
  

**Returns**:

  ``True`` if the ID is registered, ``False`` otherwise.

<a id="thirdparty.pparse.lib.pparsexml.PparseXml.get_extraction"></a>

#### PparseXml.get\_extraction

```python
def get_extraction(result_ref_id: int) -> BytesExtraction
```

Return the extraction registered for a result ID.

**Arguments**:

- `result_ref_id` - The result ID to look up.
  

**Returns**:

  The ``BytesExtraction`` associated with ``result_ref_id``.

<a id="thirdparty.pparse.lib.pparsexml.PparseXml.from_xml"></a>

#### PparseXml.from\_xml

```python
@classmethod
def from_xml(cls, xml_src: Any) -> PparseXml
```

Load and resume from a ``<pparse />`` XML document into a live object graph.

Recursively imports the root extraction, all result references, and
all ``<node />`` elements, restoring the parser and context state for
each node.

**Arguments**:

- `xml_src` - A string, file path, or XmlNode representing the
  ``<pparse />`` document root.
  

**Returns**:

  A fully populated ``PparseXml`` instance with all extraction and
  node references resolved.
  

**Raises**:

- `ValueError` - If the root element is not ``<pparse />``.
- `Exception` - If required attributes or elements are missing.

<a id="thirdparty.pparse.lib.pparsexml.ResultRef"></a>

## ResultRef

```python
class ResultRef()
```

A resolved reference to a result ID and its owning extraction.

Constructed while processing a ``<result />`` XML element; looks up the
corresponding ``BytesExtraction`` from the parent ``PparseXml`` and
stores it for downstream use.

**Arguments**:

- `pparse_xml` - The parent ``PparseXml`` that owns the result table.
- `result_xml` - The ``<result />`` XmlNode being processed.

<a id="thirdparty.pparse.lib.pparsexml.ContextRef"></a>

## ContextRef

```python
class ContextRef()
```

A bundle of the context needed when reconstructing a node from XML.

Carries the ``ResultRef`` (which provides access to the extraction), the
raw context XML element, and the resolved parser so that
``Node.from_xml`` does not need to resolve them itself.

**Arguments**:

- `result_ref` - The ``ResultRef`` that connects this context to its
  extraction and ``PparseXml``.
- `context_xml` - The ``<context />`` XmlNode for this node, if present.
- `parser` - The ``Parser`` to use when rebuilding the node, if known.

<a id="thirdparty.pparse.lib.tensor"></a>

# tensor

Abstract base class for tensor values parsed from binary model formats.

<a id="thirdparty.pparse.lib.tensor.Tensor"></a>

## Tensor

```python
class Tensor()
```

Abstract representation of a typed, shaped tensor stored in a binary format.

Subclasses are expected to implement all abstract methods to provide
access to the underlying data in a format-independent way.  The class
also provides lookup tables that map pparse dtype strings to struct
format characters, element sizes, and NumPy dtypes.

<a id="thirdparty.pparse.lib.tensor.Tensor.get_type"></a>

#### Tensor.get\_type

```python
def get_type() -> str
```

Return the safetensors-compatible dtype string for this tensor.

**Returns**:

  A dtype string such as ``"F32"``, ``"I64"``, etc.

<a id="thirdparty.pparse.lib.tensor.Tensor.get_shape"></a>

#### Tensor.get\_shape

```python
def get_shape() -> List[int]
```

Return the safetensors-compatible shape of this tensor.

**Returns**:

  A list of dimension sizes, e.g. ``[batch, channels, height, width]``.

<a id="thirdparty.pparse.lib.tensor.Tensor.get_data_bytes"></a>

#### Tensor.get\_data\_bytes

```python
def get_data_bytes() -> bytes
```

Return the raw tensor data exactly as it appears in the source.

**Returns**:

  The raw byte content of the tensor.

<a id="thirdparty.pparse.lib.tensor.Tensor.as_array"></a>

#### Tensor.as\_array

```python
def as_array() -> Any
```

Return the tensor data as a Python array typed to the tensor's dtype.

**Returns**:

  A Python sequence or array object representing the tensor values.

<a id="thirdparty.pparse.lib.tensor.Tensor.as_numpy"></a>

#### Tensor.as\_numpy

```python
def as_numpy() -> numpy.ndarray
```

Return the tensor data as a NumPy array typed to the tensor's dtype and shape.

**Returns**:

  A ``numpy.ndarray`` with the appropriate dtype and shape.

<a id="thirdparty.pparse.lib.reader"></a>

# reader

Reader interface and Cursor/Range implementations for binary data access.

<a id="thirdparty.pparse.lib.reader.Reader"></a>

## Reader

```python
class Reader()
```

Abstract interface for objects that provide sequential and random access to binary data.

Both ``Cursor`` and ``Range`` implement this interface.  Callers should
program to ``Reader`` wherever possible so that either concrete type can
be substituted transparently.

<a id="thirdparty.pparse.lib.reader.Reader.dup"></a>

#### Reader.dup

```python
def dup() -> Reader
```

Return an independent copy of this reader positioned at the same offset.

**Returns**:

  A new ``Reader`` at the current position.

<a id="thirdparty.pparse.lib.reader.Reader.tell"></a>

#### Reader.tell

```python
def tell() -> int
```

Return the current absolute byte offset within the data source.

**Returns**:

  The current read position as a byte offset.

<a id="thirdparty.pparse.lib.reader.Reader.seek"></a>

#### Reader.seek

```python
def seek(offset: int) -> Any
```

Move the read position to an absolute byte offset.

**Arguments**:

- `offset` - Target byte offset within the data source.
  

**Returns**:

  Implementation-defined; typically the resolved offset.

<a id="thirdparty.pparse.lib.reader.Reader.skip"></a>

#### Reader.skip

```python
def skip(length: int) -> Any
```

Advance the read position by ``length`` bytes without reading.

**Arguments**:

- `length` - Number of bytes to skip forward.
  

**Returns**:

  Implementation-defined; typically the new offset.

<a id="thirdparty.pparse.lib.reader.Reader.peek"></a>

#### Reader.peek

```python
def peek(length: int) -> bytes
```

Read ``length`` bytes starting at the current position without advancing.

**Arguments**:

- `length` - Number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.reader.Reader.read"></a>

#### Reader.read

```python
def read(length: int) -> bytes
```

Read ``length`` bytes and advance the read position accordingly.

**Arguments**:

- `length` - Number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.reader.Cursor"></a>

## Cursor

```python
class Cursor(Reader)
```

A stateful read pointer into a ``Data`` object.

``Cursor`` tracks the current byte offset and uses ``Data`` backend
for all I/O access.  It does not enforce any boundary; that
responsibility belongs to ``Range``.

**Arguments**:

- `data` - The backing ``Data`` object that performs actual I/O.
- `offset` - Initial byte offset within ``data``.

<a id="thirdparty.pparse.lib.reader.Cursor.cursor"></a>

#### Cursor.cursor

```python
def cursor() -> Cursor
```

Return this cursor itself. (Maintains ignorance about Cursor vs Range.)

**Returns**:

  ``self``.

<a id="thirdparty.pparse.lib.reader.Cursor.dup"></a>

#### Cursor.dup

```python
def dup() -> Cursor
```

Return a new ``Cursor`` at the current position by calling ``Data.open``.

**Returns**:

  A new ``Cursor`` positioned at the current offset.

<a id="thirdparty.pparse.lib.reader.Cursor.tell"></a>

#### Cursor.tell

```python
def tell() -> int
```

Return the current byte offset within the ``Data`` source.

**Returns**:

  The current read position.

<a id="thirdparty.pparse.lib.reader.Cursor.seek"></a>

#### Cursor.seek

```python
def seek(offset: int) -> Any
```

Move this cursor to an absolute byte offset.

**Arguments**:

- `offset` - Target byte offset.
  

**Returns**:

  The value returned by the underlying ``Data.seek`` call.

<a id="thirdparty.pparse.lib.reader.Cursor.skip"></a>

#### Cursor.skip

```python
def skip(length: int) -> Any
```

Advance this cursor by ``length`` bytes without reading any data.

**Arguments**:

- `length` - Number of bytes to skip forward.
  

**Returns**:

  The value returned by the underlying ``Data.seek`` call.

<a id="thirdparty.pparse.lib.reader.Cursor.peek"></a>

#### Cursor.peek

```python
def peek(length: int) -> bytes
```

Read ``length`` bytes at the current position without advancing the cursor.

**Arguments**:

- `length` - Number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes starting at the current offset.

<a id="thirdparty.pparse.lib.reader.Cursor.read"></a>

#### Cursor.read

```python
def read(length: int, mode: Any = None) -> bytes
```

Read ``length`` bytes.

**Arguments**:

- `length` - Max number of bytes to read.
- `mode` - Optional read mode hint passed to the backing data source.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.reader.Range"></a>

## Range

```python
class Range(Reader)
```

A bounded view over a region of a data source.

``Range`` wraps a ``Cursor`` and clamps all seek, skip, peek, and read
operations to the half-open interval ``[start, start + length]``.  It
does not duplicate any I/O logic — all actual data access is delegated
to the underlying cursor.

**Arguments**:

- `cursor` - A ``Cursor`` whose current position marks the start of the range.
- `length` - Number of bytes in the range.  Must be >= 0.
- `offset` - If >= 0, the initial read position within the range; otherwise
  defaults to the start of the range.
  

**Raises**:

- `ValueError` - If ``length`` is negative.

<a id="thirdparty.pparse.lib.reader.Range.cursor"></a>

#### Range.cursor

```python
def cursor() -> Cursor
```

Return an independent copy of the internal read cursor.

**Returns**:

  A duplicated ``Cursor`` at the current read position.

<a id="thirdparty.pparse.lib.reader.Range.dup"></a>

#### Range.dup

```python
def dup() -> Range
```

Return an independent copy of this range at the same read position.

**Returns**:

  A new ``Range`` with the same bounds and current offset.

<a id="thirdparty.pparse.lib.reader.Range.truncate"></a>

#### Range.truncate

```python
def truncate(new_length: int) -> Range
```

Shorten the range to ``new_length`` bytes.

**Arguments**:

- `new_length` - The replacement length.  Must be &lt= the current length,
  and the current read position must not fall inside the truncated
  region.
  

**Returns**:

  ``self``, to allow chaining.
  

**Raises**:

- `Exception` - If ``new_length`` exceeds the current length.
- `Exception` - If the current cursor position would be inside the
  truncated region.

<a id="thirdparty.pparse.lib.reader.Range.length"></a>

#### Range.length

```python
def length() -> int
```

Return the total number of bytes in this range.

**Returns**:

  The range length in bytes.

<a id="thirdparty.pparse.lib.reader.Range.left"></a>

#### Range.left

```python
def left() -> int
```

Return the number of bytes remaining between the cursor and the end of the range.

**Returns**:

  Bytes available to read from the current position.

<a id="thirdparty.pparse.lib.reader.Range.valid_offset"></a>

#### Range.valid\_offset

```python
def valid_offset(offset: int) -> bool
```

Return whether ``offset`` falls within ``[start, end]`` (inclusive).

**Arguments**:

- `offset` - Absolute byte offset to test.
  

**Returns**:

  ``True`` if the offset is within bounds, ``False`` otherwise.

<a id="thirdparty.pparse.lib.reader.Range.tell"></a>

#### Range.tell

```python
def tell() -> int
```

Return the current absolute byte offset of the internal cursor.

**Returns**:

  The current read position.

<a id="thirdparty.pparse.lib.reader.Range.seek"></a>

#### Range.seek

```python
def seek(offset: int) -> int
```

Move the read position to an absolute offset, clamped to the range bounds.

If ``offset`` is outside ``[start, end]`` it is clamped to the nearest
boundary rather than raising an error.

**Arguments**:

- `offset` - Target absolute byte offset.
  

**Returns**:

  The (possibly clamped) offset that was actually ``seek``ed.

<a id="thirdparty.pparse.lib.reader.Range.skip"></a>

#### Range.skip

```python
def skip(length: int) -> Any
```

Advance the read position by up to ``length`` bytes, clamped to bounds.

**Arguments**:

- `length` - Max number of bytes to skip forward.
  

**Returns**:

  The value returned by the underlying cursor's skip call.

<a id="thirdparty.pparse.lib.reader.Range.peek"></a>

#### Range.peek

```python
def peek(length: int) -> bytes
```

Read up to ``length`` bytes at the current position without advancing.

The byte count is clamped so it does not exceed the range boundary.

**Arguments**:

- `length` - Max number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.

<a id="thirdparty.pparse.lib.reader.Range.read"></a>

#### Range.read

```python
def read(length: int) -> bytes
```

Read up to ``length`` bytes.

The byte count is clamped so it does not exceed the range boundary.

**Arguments**:

- `length` - Max number of bytes to read.
  

**Returns**:

  Up to ``length`` bytes of data.



---


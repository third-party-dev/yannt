# Assume defined: PROJ_PATH, CONFIG_NAME, CONFIG_PATH

export ML_VENV_NAME=${ML_VENV_NAME:-"yolo-docker"}

export CRI_PROJ_PATH=${CRI_PROJ_PATH:-"/work"}
export CRI_BIN=${CRI_BIN:-docker}
export CRI_RUN_ARGS=${CRI_RUN_ARGS:-" "}
#!/usr/bin/env bash

{{YOU_CAN_EDIT_THIS_FILE}}

# --- run-venv.sh.j2 ---
# Note: Designed to work with both linux and wine.

# echo RUNNING $0 $@

NEXT_RUN_TARGET=${NEXT_RUN_TARGET:-"{{next_run_target}}"}

# If there is a NEXT_RUN_TARGET set, run it.
[ -z "${NEXT_RUN_TARGET}" ] || ./${NEXT_RUN_TARGET}